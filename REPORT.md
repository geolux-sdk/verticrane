# 코드 리뷰 보고서

작성일: 2026-08-28
대상: Verticrane 무인 기울기 기록기 — 저장소 전체 (6,253줄)
상태: **6건 중 4건 조치 완료.** 남은 2건은 실사용 위험 없음

## 요약

기록 경로의 내구성 설계는 견고하다. 블록 단위 `fsync`, 블록마다 CRC32, 전원 차단 후
`.partial` 복구, 확정된 파일에 다시 쓰지 않는 규칙이 서로 맞물려 있다.

이번 검토에서 **새로 확인된 치명적 결함은 없었다.** 나온 6건 중 4건을 고쳤고, 각각
회귀 테스트로 고정했다. 고친 것들의 공통점은 **정상 동작 중에는 드러나지 않고 특정
상황에서만 나타난다**는 점이다 — 카드가 고장났을 때, 슬롯이 한 바퀴 돌았을 때, 창이
닫혔어야 할 때.

## 검토 범위

| 대상 | 깊이 |
| --- | --- |
| `recorder.py` (896) | 전체 — 상태 기계, 기록 루프, 실패 경로 |
| `ahrs_file.py` (673) | 전체 — 포맷, 슬롯, 복구, 병합 |
| `web/routes.py` (390), `web/__init__.py` | 전체 — 라우트, Range, 동시성 |
| `filestore.py` (249) | 전체 |
| `eink_panel.py` (575), `gdey0154d67.py` (214) | 전체 — 스레드, 자원 해제, BUSY 대기 |
| `collect.py` (199), `app_config.py` (173), `stability.py` (352) | 전체 |
| `bt/` (1,063) | **표면 검토만** — 자격 증명 처리와 특성 보안 플래그 위주 |
| `hwt9037_485.py`, `read_status.py`, `port_config.py` | 표면 검토만 |

실기기(`pi-tilt001`)에서 확인: 부팅 순서, Range 응답, 40% 지점에서 끊고 이어받은 파일의
sha256 일치, 수집 후 장비 상태, BLE 광고 창의 개폐, 수정 후 정상 기록.

---

# 조치 완료

## ① BLE 상시 노출 → 부팅 후 3분만 광고

**문제** — 캐릭터리스틱이 `["write", "write-without-response"]` 로 열려 있어 페어링도
암호화도 없는데, 서비스가 systemd 로 상시 기동(`enabled` + `active`)되어 있었다.

`bt/README.md` 의 위험 평가가 사실과 어긋났다. 문서는 *"설정 시점에만 쓰는 채널이라
위험 노출 시간은 짧다"* 고 적었지만, 실제 노출은 **장비가 켜져 있는 전 시간**이었다.

**영향은 둘이고, 문서는 첫 번째만 다뤘다.**

- 비밀번호 노출 — `{"cmd":"secret"}` 가 저장된 PSK 를 평문으로 돌려준다.
- **장비를 못 쓰게 만들 수 있다** — `connect` 로 존재하지 않는 SSID 로 옮겨버리면,
  화면도 키보드도 없는 크레인 위 장비에서 데이터를 가져올 방법이 사라진다. 되돌릴
  유일한 경로가 방금 공격에 쓰인 그 BLE 다.

두 번째가 더 무겁다. 첫 번째는 이미 차량 Wi-Fi 에 들어온 사람에게 큰 이득이 없지만
(API 가 어차피 무인증), 두 번째는 **접근 권한이 전혀 없는 사람이 측정 전체를 무산시킬
수 있는 경로**다.

**조치** — 광고 창을 부팅 후 `ADV_WINDOW_S`(기본 180초)로 제한했다. 창이 닫히면 아무도
장비를 찾지 못하고, 다시 열려면 전원을 껐다 켜야 한다 — 접속이 안 될 때 운영자가 어차피
하는 일이다. 문서가 원래 주장하던 전제를 실제로 만든 셈이다.

**설정 중에는 닫히지 않는다.** 창을 닫는 검사가 "붙어 있는 호스트가 없다" 분기 안에만
있어, 3분이 지나도 진행 중인 세션은 끝까지 간다. 값은 유닛 파일의
`VERTICRANE_BLE_WINDOW_S` 로 바꾼다 — 3분은 **이미 폰을 들고 있는 사람에게만** 충분한
시간이라 현장에서 조정이 필요할 수 있다.

`bt/README.md` 의 보안 절도 다시 썼다. 장비 무력화 위험과, 더 강하게 갈 때의 선택지
(**페어링 패스키를 e-paper 에 표시**하는 본딩 — 화면을 볼 수 있는 사람은 이미 장비 앞에
있는 사람이다)를 근거와 함께 남겼다.

## ② `Writer` 이어붙이기 → 항상 새로 쓰기

**문제** — `Writer.__init__` 이 파일이 이미 있으면 `"r+b"` 로 열고 끝으로 seek 했다.
슬롯이 한 바퀴 돈 자리에 복구 안 된 `.partial` 이 남아 있으면 **헤더 하나에 서로 다른 두
기록의 블록**이 담긴다. 헤더의 시작 시각은 앞 기록 것이고 뒤 블록들의 `elapsed_ms` 는
0부터 다시 시작하는데, **블록 CRC 는 각각 맞으므로 `scan()` 도 목록도 통과한다.** 어떤
검사도 못 걸러내는 오염이었다.

**조치** — 항상 `"w+b"`. 회전이 곧 덮어쓰기라는 §5.1 규칙을 한 단계 앞에 적용한 것이고,
사라진 `else` 분기를 원하던 호출자는 없었다.

## ③ 슬롯 소모 → 실패 시 반납 + 연속 5회면 중지

**문제** — `_start_recording()` 이 파일을 열기 전에 슬롯을 뗐다. 열기에 실패하면 상태가
`waiting_stable` 에 남아 1초 뒤 다시 시도하고, 매번 슬롯을 하나씩 소모하며 이미 쓰기가
실패하는 카드에 `.slot` 을 다시 썼다. 17분이면 1000개를 한 바퀴 돈다.

**처음 세운 방안이 틀렸다.** "열기에 성공한 뒤에 슬롯을 확정한다" 로 가려 했는데, 그러면
이런 일이 생긴다.

```
부팅 복구가 TOP_005.dat.partial → TOP_005.dat 으로 확정
.slot 은 아직 4          (확정 전이라 올라가지 않음)
다음 기록이 슬롯 5를 받음 → TOP_005.dat 을 덮어씀
                           ↑ 방금 복구한 파일이 사라진다
```

**슬롯을 먼저 떼는 것은 그 자체로 필요한 순서**였다. 복구가 남은 `.partial` 을 이미 쓰고
있던 슬롯으로 확정하므로, 카운터가 그 번호를 지나 있지 않으면 다음 기록이 방금 구한
파일을 덮는다.

**조치** — 순서는 그대로 두고 **실패했을 때만 되돌린다**(`release_slot()`). 열기가
실패해 그 이름으로 아무것도 쓰이지 않았으므로 반납이 안전하다. 연속 `OPEN_FAILURE_LIMIT`
(5회) 실패하면 `maintenance` 로 내려 재시도를 멈춘다 — 5회면 순간적인 오류는 넘어가고
진짜 고장은 잡는다. 성공하면 횟수가 0으로 초기화되므로, 한 번 삐끗한 카드가 부팅 내내
"포기 직전" 으로 남지 않는다.

## ④ `snapshot()` 이 복사본이 아님 → 복사본 반환

**문제** — 살아 있는 `Status` 객체를 그대로 돌려주고, `_poll_once()` 는 락 없이 그 필드를
갱신했다. 락이 읽는 쪽만 잡고 있어 아무것도 보호하지 못했고, 웹과 패널이 받는 것은
스냅샷이 아니라 기록기가 계속 고치는 그 객체였다. 잘못된 값이 나오지는 않지만 **한 화면
안의 필드들이 서로 다른 순간의 것일 수 있었다.**

**조치** — `copy.copy()` 로 얕은 복사본을 돌려준다. `dataclasses.replace()` 대신 쓴 것은
`__init__` 을 다시 타지 않기 때문이고, 얕은 복사로 충분한 것은 모든 필드가 스칼라이거나
(`config_warnings`·`stability`) 제자리 수정 없이 통째로 교체되기 때문이다.

---

# 남은 것

둘 다 **실사용 위험이 없다.** 지금처럼 파일이 몇 개일 때는 무시할 만하고, 다음에 그
파일을 건드릴 때 함께 하면 된다.

## ⑤ [낮음] 페이지 한 번 그릴 때 디렉터리를 두 번 훑는다

`web/routes.py` 의 `index` 가 `list_files()` 와 `stats()` 를 부르는데 `stats()` 안에서
`list_files()` 를 다시 부른다. `/api/files` 도 같다. 그리고 `list_files()` 는 파일마다
헤더와 마지막 블록을 읽고, `describe()` 가 `quick_summary()` 와 별개로 헤더를 한 번 더
읽는다 — 파일 하나당 읽기 3회를 페이지마다 2배로 한다.

회수가 밀려 수백 개가 쌓이면 Zero 2 W 의 SD 카드에서 눈에 띄고, 목록 조회가 기록 루프와
I/O 를 다툰다. **조치안**: `stats()` 가 이미 만든 목록을 받도록 시그니처를 바꾸고,
`describe()` 가 `quick_summary()` 의 헤더를 재사용한다. 둘 다 국소적이다.

## ⑥ [정보] `_send()` 의 다중 파일 경로는 호출되지 않는다

`_send(paths: list[str], ...)` 가 여러 파일을 이어 보내도록 되어 있지만 부르는 두 곳 모두
길이 1의 리스트를 넘긴다(그룹 다운로드는 먼저 임시 파일로 병합한다). Range 처리도 그
다중 파일 경로를 따라 쓰여 있어, **실행되지 않는 분기에 오프셋 계산이 들어 있다.**

해가 되지는 않으나 검증되지 않는 코드가 남아 있다는 뜻이다. 단일 경로로 줄이면 Range
계산도 눈에 띄게 단순해진다.

---

# 문제없다고 확인한 것

되짚어 본 것 중 정상으로 판단한 것들이다. 다음 검토에서 같은 자리를 다시 파지 않도록
적어 둔다.

- **`_check_disk()` 의 재진입** — `_flush_block()` 안에서 `_stop_recording()` 을 부르지만
  그 시점에 `_pending` 이 이미 비어 있어 중첩된 `_flush_block()` 이 즉시 반환한다.
  앞서 고친 쓰기 실패 결함과 같은 모양이지만 여기서는 종료한다.
- **`_maybe_segment()` 가 중지 직후 다시 시작하는가** — `_stop_recording()` 이 끝에서
  `_record_start_mono = 0.0` 으로 두므로 `_maybe_segment()` 가 곧바로 반환한다.
- **다운로드 중 파일이 휴지통으로 옮겨질 때** — `os.replace` 는 열린 fd 의 inode 를
  건드리지 않으므로 전송이 끝까지 간다. `threaded=True` 인 웹 서버에서도 안전하다.
- **패널 BUSY 대기** — 10초 타임아웃이 있고 `close()` 가 SPI 와 GPIO 를 모두 놓는다.
  패널 실패는 기록을 멈추지 않는다.
- **복구 시 헤더 재기록** — `"r+b"` 는 0에서 시작하므로 64바이트가 제자리에 덮이고 CRC 도
  `pack()` 에서 다시 계산된다. 중간에 전원이 끊겨도 다음 부팅이 같은 일을 반복할
  뿐이다(멱등).
- **안정화 윈도우** — 시간 기준으로 앞을 버리므로 무한히 자라지 않는다.
- **`_local_ip()` 를 샘플 루프에서 부르는 것** — 실측 **0.06 ms**. 10초에 한 번이므로
  25 Hz 폴링에 영향이 없다.
- **PIN** — 설정된 사안이다(`app_config.py` 의 해당 줄 주석 참고). 보안 지적 대상이
  아니다.

---

# 회귀 테스트

고친 4건 중 3건이 `test_recorder.py` 로 고정됐다. **모두 이전 코드에서 실패한다.**

```
[1] 쓰기 실패     프로세스 생존 · 한 번만 시도 · writer 닫힘 · 샘플 폐기 · 오류 기록
[2] 덜 찬 블록    쓰지 않음 · 기록 계속 · 모아둔 것 비움
[3] 열기 실패     슬롯 제자리 · 한계 넘으면 중지 · 복구되면 초기화
[4] 슬롯 재사용   이어붙지 않음 · 두 번째 헤더가 남음 · 블록 수가 맞음
[5] 스냅샷        복사본임 · 찍힌 순간의 값 유지
```

①(BLE 창)은 하드웨어 의존이라 자동 테스트가 없다. 실기기에서 창이 열리고 12초 뒤 닫히는
것을 로그로 확인했다.

`./test.sh` 가 포맷(82) · 안정화(33) · 패널 · 기록기 네 묶음을 모두 실행한다.
