# 코드 리뷰 보고서

작성일: 2026-08-28
대상: Verticrane 무인 기울기 기록기 — 저장소 전체 (6,253줄)

## 요약

기록 경로의 내구성 설계는 견고하다. 블록 단위 `fsync`, 블록마다 CRC32, 전원 차단 후
`.partial` 복구, 확정된 파일에 다시 쓰지 않는 규칙이 서로 맞물려 있고, 오늘 회전형
슬롯 이름으로 바꾸면서 "이름이 사실을 담지 않는다"는 원칙이 코드와 문서 양쪽에
반영됐다.

새로 확인된 **치명적 결함은 없다.** 앞선 보고서의 재귀 결함은 수정되고 회귀 테스트로
고정됐다. 이번 검토에서 나온 것 중 조치가 필요한 것은 두 건이며, 둘 다 **정상 동작
중에는 드러나지 않고 특정 상황에서만 나타나는 종류**다.

## 검토 범위

읽고 대조한 것:

| 대상 | 깊이 |
| --- | --- |
| `recorder.py` (896) | 전체 — 상태 기계, 기록 루프, 실패 경로 |
| `ahrs_file.py` (673) | 전체 — 포맷, 슬롯, 복구, 병합 |
| `web/routes.py` (390), `web/__init__.py` | 전체 — 라우트, Range, 동시성 |
| `filestore.py` (249) | 전체 |
| `eink_panel.py` (575), `gdey0154d67.py` (214) | 전체 — 스레드, 자원 해제, BUSY 대기 |
| `collect.py` (199), `app_config.py` (173), `stability.py` (352) | 전체 |
| `bt/` (1,063) | **표면 검토만** — 자격 증명 처리와 특성 보안 플래그 위주 |
| `hwt9037_485.py`, `read_status.py`, `port_config.py` | 표면 검토만 |

실기기(`pi-tilt001`)에서 확인한 것: 부팅 순서, Range 응답, 40% 지점에서 끊고 이어받은
파일의 sha256 일치, 수집 후 장비 상태, BLE 서비스 상시 동작 여부.

---

## 발견 사항

### 1. [중간] 슬롯이 한 바퀴 돈 자리에 `.partial`이 남아 있으면 두 기록이 한 파일로 이어붙는다

**근거** — `ahrs_file.py`의 `Writer.__init__`:

```python
new: bool = not os.path.exists(path)
self._f = open(path, "w+b" if new else "r+b")
if new:
    self._f.write(header.pack())
    ...
else:
    self._f.seek(0, os.SEEK_END)      # 이어붙인다
```

**영향** — 이어붙인 파일은 **헤더 하나에 서로 다른 두 기록의 블록**을 담는다. 헤더의
시작 시각은 앞의 기록 것이고, 뒤 블록들의 `elapsed_ms`는 자기 기준으로 0부터 다시
시작한다. CRC는 블록마다 맞으므로 `scan()`도 통과하고, 목록에도 정상으로 뜬다.
**어떤 검사도 걸러내지 못하는 조용한 오염**이다.

**도달 가능성은 낮다.** 부팅 복구가 모든 `.partial`을 처리하고 슬롯은 1000개마다
돌아오므로, 복구가 실패해 남은 `.partial` 위로 1000번째 기록이 정확히 겹쳐야 한다.

**다만 `else` 분기를 원하는 호출자가 없다.** `Writer(...)`를 부르는 곳은
`_start_recording()`과 테스트뿐이고 둘 다 새 파일을 기대한다. 이어붙이기를 쓰는 코드가
없는데 이어붙이기 경로만 남아 있다.

**권고** — 항상 `w+b`로 열어 덮어쓴다. 회전이 곧 덮어쓰기라는 §5.1의 규칙과도 일치하고,
사라지는 것은 아무도 쓰지 않는 분기뿐이다.

---

### 2. [중간] BLE 프로비저닝이 상시 동작하며, 인증도 암호화도 없다

**근거** — `bt/ble_peripheral.py:585`:

```python
ble.add_characteristic(..., flags=["write", "write-without-response"], ...)
```

`encrypt-write`도 본딩 요구도 없다. 그리고 실기기에서:

```
systemctl is-enabled pi-bt-wifi-setup  → enabled
systemctl is-active  pi-bt-wifi-setup  → active
```

**`bt/README.md`의 위험 평가가 이 사실과 어긋난다.** 문서는 이렇게 적는다:

> 설정 시점에만 쓰는 채널이라 **위험 노출 시간은 짧지만**…

그러나 서비스는 systemd로 상시 기동되어 현장에 있는 내내 광고한다. 노출 시간은 짧지
않고 **장비가 켜져 있는 전 시간**이다.

**영향 두 가지.** 문서는 첫 번째만 다룬다.

- **비밀번호 노출** — `{"cmd":"secret","ssid":".."}`가 저장된 PSK를 평문으로 돌려준다
  (`ble_peripheral.py:295`). 근처의 누구나 차량 Wi-Fi 비밀번호를 읽을 수 있다.
- **장비를 못 쓰게 만들 수 있다** — 같은 채널로 `connect`를 써서 **다른 SSID나 존재하지
  않는 SSID로 옮겨버릴 수 있다.** 이 장비는 화면도 키보드도 USB 콘솔도 없고 크레인
  위에 있다. 되돌릴 유일한 경로가 방금 공격에 쓰인 그 BLE다. 데이터가 온전해도
  가져올 방법이 사라진다.

두 번째가 더 무겁다. 첫 번째는 이미 차량 Wi-Fi에 접근할 수 있는 사람에게 별 이득이
없지만(§5의 API가 어차피 무인증), 두 번째는 **접근 권한이 전혀 없는 사람이 측정 전체를
무산시킬 수 있는 경로**다.

**권고** — 셋 중 하나. 어느 쪽이든 망 구성 결정과 함께 정하는 것이 맞다.

1. 특성에 `encrypt-write` + LE Secure Connections 본딩을 요구한다 (README가 이미 제안)
2. 서비스를 상시로 두지 않는다 — 부팅 후 N분만 광고하거나, 물리 버튼/GPIO로 켠다
3. `connect`와 `secret`에만 공유 비밀을 요구한다 (`scan`·`status`는 열어 둠)

2번이 제일 싸고, 문서의 원래 전제("설정 시점에만")를 실제로 만드는 방법이다.

---

### 3. [낮음] 카드가 가득 차면 슬롯 번호를 초당 하나씩 소모한다

**근거** — `_start_recording()`은 파일을 열기 **전에** 슬롯을 뗀다:

```python
name = af.build_filename(self.position, af.next_slot(self.data_dir), partial=True)
...
try:
    self.writer = af.Writer(path, header, ...)
except OSError as exc:
    logger.error("Cannot open {}: {}", name, exc)
    self.status.error = str(exc)
    return                    # 상태가 waiting_stable 로 남는다
```

상태가 `waiting_stable`에 머물므로 `_tick()`이 다음 안정 판정(1초 뒤)에 다시 부른다.

**영향** — 실패할 때마다 `data/.slot`에 쓴다. 이미 쓰기가 실패하는 카드에 **초당 한
번씩 쓰기를 시도**하고, 17분이면 슬롯 1000개를 한 바퀴 돌린다. 그동안 로그에는 같은
오류가 초당 하나씩 쌓인다.

**권고** — 열기에 실패하면 슬롯을 되돌리거나(카운터를 쓰기 성공 뒤로 미루거나),
`_check_disk()`가 하는 것처럼 `maintenance`로 내려 재시도를 멈춘다. 후자가 §11의
"모자라면 경고 후 기록을 중지한다"와 일치한다.

---

### 4. [낮음] `snapshot()`은 복사본이 아니고, `_lock`은 아무것도 보호하지 않는다

**근거** — `recorder.py`:

```python
def snapshot(self) -> Status:
    with self._lock:
        self.status.state = self.state
        ...
        return self.status        # 살아 있는 객체를 그대로 돌려준다
```

`_poll_once()`가 `self.status.tilt` 등을 **락 없이** 갱신하므로, 락은 읽는 쪽만 잡는다.

**영향** — 웹과 패널이 받아 가는 것은 스냅샷이 아니라 기록기가 계속 고치고 있는 그
객체다. CPython에서 개별 필드는 찢어지지 않으므로 잘못된 값이 나오지는 않지만,
**한 화면 안의 필드들이 서로 다른 순간의 것일 수 있다**(예: `samples`는 갱신 전,
`elapsed_s`는 갱신 후). 상태 표시에는 무해하다.

**권고** — 실질적 위험은 없으나 이름과 락이 둘 다 있지도 않은 보장을 암시한다.
`dataclasses.replace()`로 복사본을 돌려주거나, 락을 걷고 주석으로 사실을 적는다.
전자가 낫다 — 값이 싸고, 뒤에 붙는 코드가 스냅샷을 스냅샷으로 믿을 수 있다.

---

### 5. [낮음] 페이지 한 번 그릴 때 디렉터리를 두 번 훑는다

**근거** — `web/routes.py`의 `index`:

```python
files=filestore.list_files(_data_dir(), _gap()),
stats=filestore.stats(_data_dir()),      # stats() 안에서 list_files() 를 다시 부른다
```

`/api/files`도 같다. 그리고 `list_files()`는 파일마다 헤더와 **마지막 블록**을 읽고,
`describe()`가 `quick_summary()`와 별개로 헤더를 한 번 더 읽는다.

**영향** — 파일 하나당 디스크 읽기 3회, 그것을 페이지마다 2배로 한다. 지금처럼 파일이
몇 개일 때는 무시할 만하지만, 회수가 밀려 수백 개가 쌓이면 Zero 2 W의 SD 카드에서
눈에 띈다. 목록 조회 자체가 기록 루프와 CPU·I/O를 다툰다.

**권고** — `stats()`가 이미 만든 목록을 받도록 시그니처를 바꾸고, `describe()`가
`quick_summary()`의 헤더를 재사용한다. 둘 다 국소적이다.

---

### 6. [정보] `_send()`의 다중 파일 경로는 호출되지 않는다

`_send(paths: list[str], ...)`가 여러 파일을 이어 보내도록 되어 있지만, 부르는 두 곳
모두 **길이 1의 리스트**를 넘긴다(그룹 다운로드는 먼저 임시 파일로 병합한다). 오늘
추가한 Range 처리도 그 다중 파일 경로를 따라 쓰여 있어, 실행되지 않는 코드에 오프셋
계산이 들어 있다.

해가 되지는 않으나, 검증되지 않는 분기가 남아 있다는 뜻이다. 단일 경로로 줄이면
Range 계산도 눈에 띄게 단순해진다.

---

## 문제없다고 확인한 것

되짚어 본 것 중 정상으로 판단한 것들이다. 다음 검토에서 같은 자리를 다시 파지 않도록
적어 둔다.

- **`_check_disk()`의 재진입** — `_flush_block()` 안에서 `_stop_recording()`을 부르지만,
  그 시점에 `_pending`이 이미 비어 있어 중첩된 `_flush_block()`이 즉시 반환한다.
  1번 결함과 같은 모양이지만 여기서는 종료한다.
- **`_maybe_segment()`가 중지 직후 다시 시작하는가** — `_stop_recording()`이 끝에서
  `_record_start_mono = 0.0`으로 두므로 `_maybe_segment()`가 곧바로 반환한다.
- **다운로드 중 파일이 휴지통으로 옮겨질 때** — `os.replace`는 열린 fd의 inode를
  건드리지 않으므로 전송은 끝까지 간다. `threaded=True`인 웹 서버에서도 안전하다.
- **패널 BUSY 대기** — 10초 타임아웃이 있고 `close()`가 SPI와 GPIO를 모두 놓는다.
  패널 실패는 기록을 멈추지 않는다.
- **복구 시 헤더 재기록** — `"r+b"`는 0에서 시작하므로 64바이트가 제자리에 덮이고
  CRC도 `pack()`에서 다시 계산된다. 중간에 전원이 끊겨도 다음 부팅이 같은 일을
  반복할 뿐이다(멱등).
- **안정화 윈도우** — 시간 기준으로 앞을 버리므로 무한히 자라지 않는다.
- **`_local_ip()`를 샘플 루프에서 부르는 것** — 실측 **0.06 ms**. 10초에 한 번이므로
  25 Hz 폴링에 영향이 없다.
- **PIN** — 설정된 사안이다(`app_config.py`의 주석 참고). 보안 지적 대상이 아니다.

---

## 우선순위

| | 무엇 | 왜 이 순서인가 |
| --- | --- | --- |
| 1 | **2번** BLE 상시 노출 | 접근 권한이 없는 사람이 장비를 못 쓰게 만들 수 있는 유일한 경로다. 데이터가 온전해도 가져올 방법이 사라진다 |
| 2 | **1번** `Writer` 이어붙이기 | 도달 가능성은 낮지만 도달하면 **어떤 검사도 못 잡는 오염**이고, 고치는 것은 한 줄이다 |
| 3 | **3번** 슬롯 소모 | 이미 고장난 카드를 더 두드린다. `maintenance`로 내리면 끝난다 |
| 4 | 4·5·6번 | 실사용 위험 없음. 다음에 그 파일을 건드릴 때 함께 |

1번과 3번은 지금 고칠 만하다. 2번은 망 구성(장비가 자기 AP를 여는 안)과 함께 정하는
것이 맞다 — 어느 쪽으로 가느냐에 따라 답이 달라진다.
