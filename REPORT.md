# 코드·문서 분석 보고서

작성일: 2026-08-28  
대상: Verticrane 무인 기울기 기록기 (Raspberry Pi / HWT9037-485)

## 요약

문서와 구현은 전원 차단 뒤의 `.partial` 복구, 블록 CRC, `fsync`, 안정화 판정, 현장용
파일 수집의 의도를 비교적 일관되게 구현한다. 다만 **저장 장치 쓰기 실패라는 핵심 장애
상황에서 재귀가 무한 반복되어 기록 프로세스가 비정상 종료할 수 있는 결함**이 확인됐다.
또한 기본 PIN의 운영 경로, 파일 회수 API의 인증, 자동화 테스트 범위가 현장 장비의 위험도에
비해 부족하다.

## 확인 범위와 방법

- `agent.md`, `README.md`, 구현 요구사항, 운영자 안내서, 프로토콜·Pi 설치 문서를 검토했다.
- 기록 루프, 파일 포맷/복구, 파일 저장소, Flask 라우트, 설정/PIN, systemd 단위를 대조했다.
- 핵심 Python 모듈의 컴파일을 통과했다.
- 모의 `OSError` writer로 기록 쓰기 실패 경로를 실행해 아래 1번 결함을 재현했다.
- 기본 테스트 중 `test_ahrs_file.py`는 이 Windows 샌드박스가 `tempfile.mkdtemp()`로 생성한
  디렉터리의 후속 접근을 거부하여 중단됐다. 이는 제품 코드의 실패로 판정하지 않았다.

## 발견 사항

### 1. [치명] 기록 블록 쓰기 실패가 무한 재귀와 프로세스 종료로 이어진다

- 근거: [`recorder.py`](recorder.py:653)의 `write_block()` 예외 처리에서
  `_stop_recording()`을 호출한다. 그러나 [`_stop_recording()`](recorder.py:697)은 남은
  샘플을 저장하려고 다시 `_flush_block()`을 호출한다. 실패했던 25개 샘플이 그대로 남아
  있으므로 같은 `OSError` → `_stop_recording()` 경로가 반복된다.
- 재현: 항상 `OSError`를 내는 writer를 주입했을 때 `Write failed` 로그가 반복된 뒤
  `RecursionError`가 발생했다.
- 영향: SD 카드 부족, I/O 오류, 읽기 전용 전환처럼 바로 처리해야 하는 조건에서 정상 종료·
  `.partial` 복구 경로가 깨지고 systemd 재시작 루프에 들어갈 수 있다. 이는 “조용히 기록이
  멈추는 것이 최악”이라는 프로젝트 요구와 정면으로 충돌한다.
- 권고: 쓰기 예외에서는 먼저 `_pending`을 비우고 writer를 닫는 **오류 전용 종료 경로**를
  사용하라. 종료 시 `_flush_block()`을 선택적으로 생략하도록 하거나, writer를 `None`으로
  만든 뒤 재진입하지 않게 해야 한다. 이 경로를 모의 `OSError` 테스트로 고정해야 한다.

### 2. [높음] 배포된 웹 UI에서 기본 PIN을 변경할 수 없어 알려진 기본 자격 증명이 남는다

- 근거: 기본 PIN은 [`app_config.py`](app_config.py:26)에 상수로 존재한다. 실행 중인 Flask
  설정 화면은 [`web/routes.py`](web/routes.py:89)에서 기존 PIN으로 설정 저장만 허용하며,
  PIN 변경 라우트는 없다. `/setup`을 언급하는 `app_config.py`와 `web/__init__.py`의 주석과도
  현재 실행 경로가 맞지 않는다.
- 영향: 웹 서버가 `0.0.0.0`에 바인딩되므로 같은 차량 Wi-Fi의 사용자는 알려진 기본 PIN으로
  설정 변경 또는 파일 삭제를 시도할 수 있다. PIN 변경은 개발자 전용 Streamlit 도구에만
  남아 있어, 운영자가 정상 운영 UI로 초기 PIN을 교체할 수 없다.
- 권고: Flask에 PIN 변경 기능(기존 PIN 확인, 최소 길이/재입력, rate limit)을 추가하거나,
  최초 부팅 시 기본 PIN을 거부하고 별도 초기화 절차를 강제하라. 문서와 오래된 `/setup` 주석도
  실제 경로에 맞춰 갱신해야 한다.

### 3. [중간] 파일을 ‘수집됨’으로 숨기는 POST API가 인증 없이 호출된다

- 근거: [`api_collected`](web/routes.py:199)와
  [`api_group_collected`](web/routes.py:216)는 `_pin_ok()` 검사를 하지 않고
  [`move_to_trash`](filestore.py:165)를 호출한다. 반면 실제 삭제 API만 PIN을 검사한다.
- 영향: 같은 네트워크의 임의 클라이언트 또는 사용자가 방문한 악성 페이지가 파일명을 알고
  있으면 기록을 목록에서 제거할 수 있다. 휴지통 보관 기간 동안 복구는 가능하지만, 현장
  운영자는 파일이 없다고 판단할 수 있다.
- 권고: 수집 완료 API에도 PIN 또는 다운로드 응답에서 발급한 일회성 확인 토큰을 요구하라.
  브라우저 기반 요청을 유지한다면 CSRF 방어도 함께 적용하라.

### 4. [중간] 자동 테스트가 웹·기록 상태기계·저장 오류 경로를 검증하지 않는다

- 근거: [`test.sh`](test.sh:20)는 포맷, 안정화, e-paper 테스트만 실행하고 마지막에 실제
  센서 점검을 실행한다. Flask 라우트와 `Recorder` 상태 전이는 별도 테스트 파일이 없으며,
  1번의 쓰기 실패 경로도 검증되지 않는다.
- 영향: 전원 차단 복구처럼 포맷 단위로는 안전해도, 실제 운용 경로의 상태 전이·권한·디스크
  오류 회귀를 배포 전에 발견하지 못한다.
- 권고: 하드웨어 없이 실행되는 pytest 또는 현 테스트 스타일의 테스트를 추가해 다음을
  검증하라: writer `OSError`, 디스크 여유 공간 부족, HTTP 접속 중 기록 폐기, PIN/CSRF,
  수집·휴지통·그룹 병합, 종료 시 finalise. CI에서 이 묶음을 필수로 실행한다.

### 5. [낮음] 설정 파일 원자 교체 후 디렉터리 동기화가 빠져 있다

- 근거: [`app_config.save()`](app_config.py:109)는 임시 파일을 `fsync()`한 뒤
  `os.replace()`하지만, 파일 포맷 코드가 rename 직후 수행하는 디렉터리 `fsync()`는 하지
  않는다.
- 영향: 전원 차단 타이밍에 따라 설정 변경이 사라질 수 있다. JSON이 반쯤 써지는 위험은 낮지만,
  요구사항이 설정 저장에도 “임시 파일 → fsync → 원자적 rename”의 내구성을 요구하므로
  완전한 내구성 보장은 아니다.
- 권고: `os.replace()` 뒤 부모 디렉터리를 열어 `fsync()`하고, 전원 차단 모의 시험을 추가하라.

## 우선 조치

1. 1번 재귀 결함을 수정하고 모의 I/O 오류 테스트를 추가한다.
2. 현장 장비의 PIN을 즉시 기본값이 아닌 값으로 교체하고, 웹 UI의 PIN 변경 경로를 제공한다.
3. 수집 완료 API의 요청 권한/CSRF 정책을 결정해 적용한다.
4. 웹 및 상태기계 통합 테스트를 CI 또는 `test.sh --no-hardware`에 포함한다.

