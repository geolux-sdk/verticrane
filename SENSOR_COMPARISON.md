# 센서 비교 보고서 — HWT9037-485 대 SCL3300

작성일: 2026-08-29
대상: WitMotion HWT9037-485 (현행) 대 Murata SCL3300 (검토)
장비: Raspberry Pi Zero 2 W · Modbus RTU on `/dev/serial0` + SPI0 CE1
상태: **잡음·분해능·주파수응답·스케일·절대정확도 측정 완료.** 채택 방식은 미결

## 요약

두 센서는 **이 장비가 실제로 쓰는 대역에서 사실상 동등하다.**

원시 표준편차만 비교하면 HWT9037이 20배 조용해 보인다. 그 숫자는 틀렸다. 측정값이
아니라 필터의 출력이기 때문이다 (3절). 대역폭을 맞추면 둘은 만나고, 12–14° 회전을 세 번
독립적으로 재도 스케일 차이는 검출되지 않는다 (+0.6 % ± 1.4 %, 7절).

실질적 차이는 정밀도가 아니라 **평활화에 대한 접근권**에 있다. SCL3300은 날것을
주므로 얼마나 평활할지 이쪽이 정하고, HWT9037은 이미 평활을 끝낸 값을 주므로 되돌릴
수 없다.

**그래서 답은 크레인이 어떻게 움직이느냐에 달려 있다.**

| 재려는 것 | 판정 |
| --- | --- |
| 1 % 기울기 같은 정적·준정적 각도 | **동등.** 둘 다 한두 자릿수 여유이고, 오차는 센서가 아니라 설치가 지배한다 |
| 지속적인 흔들림 | **동등.** 가장 불리한 조합조차 잡음의 252배 |
| **삐걱거림 같은 짧고 작은 외란** | **SCL3300.** HWT9037은 약 10배 큰 외란을 요구한다 |

근거는 2절에 모아 두었다. 마지막 줄이 결정적일 수 있다 — HWT9037은 2–3 Hz에서 꺾이고
작은 신호는 양자화에 막히므로, **크레인이 삐걱거리는 중에도 "안정"이라고 답할 수 있다.**
이는 [stability.py](stability.py)의 정지 판정에 직접 걸린다.

---

## 1. 측정 조건

| 항목 | 값 |
| --- | --- |
| 잡음 기준 데이터 | 150 s · 25 Hz · 3,750 샘플 · 정지 |
| 주파수 응답 데이터 | 120 s · 25 Hz · 지속 가진 (진폭 100–200 mg) |
| 스케일 데이터 | 12–14° 회전 · 정지 자세 3쌍 · 자세당 750 샘플 |
| SCL3300 설정 | 모드 3 (±1.2 g, 10 Hz LPF, 저소음) |
| HWT9037 설정 | 대역폭 10 Hz, ACCRANGE 16 g 고정 |
| 결선 | SCL3300은 SPI0 CE1, e-paper 패널이 CE0 |
| 고정 | 두 센서를 같은 아크릴 판에 부착 |
| SCL3300 식별 | WHOAMI `0xC1`, 시리얼 5100244486 |

두 센서는 서로 다른 버스에 있어 어느 쪽도 상대를 기다리지 않는다.

---

## 2. 결론 — 세 가지 용도에 대한 답

### 2.1 1 % 기울기 측정 — 둘 다 문제없다

1 % 기울기는 **0.5729°**다. 이 값 안에 들어가야 할 오차 요인을 전부 같은 단위로 늘어놓으면
어느 항목이 총합을 좌우하는지가 보인다.

| 항목 | 각도 | % 기울기 |
| --- | --- | --- |
| HWT9037 정밀도 (25 Hz 원시) | 0.0005° | 0.0009 % |
| SCL3300 정밀도 (0.5 Hz, 25 평균) | 0.0014° | 0.0024 % |
| SCL3300 정밀도 (25 Hz 원시) | 0.0097° | 0.0169 % |
| HWT9037 가속도계 실분해능 (0.488 mg) | 0.0280° | 0.0488 % |
| HWT9037 드라이버 반올림 후 (1 mg) | 0.0573° | 0.1000 % |
| **자세 재현성 (실측, 설치 오차)** | **0.2500°** | **0.4363 %** |

**두 센서 모두 필요한 것보다 한두 자릿수 여유가 있다.** 그리고 **총 오차를 정하는 것은
센서가 아니라 설치**다 — 7절에서 잰 자세 재현성 0.25°가 그 자체로 0.44 % 기울기이며,
가장 나쁜 센서 항목보다 4배 크다.

> 1 % 기울기 측정에서 센서 선택은 오차에 거의 영향을 주지 않는다. 같은 노력을 **장착
> 정밀도와 영점 관리**에 쓰는 편이 훨씬 크게 남는다.

### 2.2 지속적인 흔들림 검출 — 둘 다 두세 자릿수 여유

요구는 "책상을 흔든 정도의 외란을 확실히 감지할 것"이다. 4절의 가진을 각도로 환산하고 각
센서의 정지 잡음으로 나누면 그 여유가 나온다.

| 가진 | HWT9037이 보는 크기 | 잡음 대비 | SCL3300이 보는 크기 | 잡음 대비 |
| --- | --- | --- | --- | --- |
| 느림 (0–0.35 Hz) | 11.67° rms | **23,000배** | 12.02° rms | **1,240배** |
| 중간 (0.7–1.4 Hz) | 6.52° rms | 13,000배 | 6.82° rms | 703배 |
| 빠름 (2.8–5.6 Hz) | 1.09° rms | 2,180배 | 2.44° rms | 252배 |

가장 불리한 조합(SCL3300, 빠른 가진)조차 잡음의 252배다. HWT9037이 빠른 성분을 2.24배
깎아내지만 그래도 자기 잡음의 2,180배로 보고 있다.

**이 요구사항만 놓고 보면 두 센서는 교체 가능하다.** 선택 근거는 검출 능력이 아니라 다른
곳에 있어야 한다 — 자이로 유무, 통합 비용, 부품 수급 같은 것들.

### 2.3 짧고 작은 외란 — 여기서만 갈린다

4절에서 잰 두 한계(2–3 Hz 코너, 0.488 mg 양자화)가 함께 작용한다. 삐걱거림은 짧고 빠르고
작다 — 세 조건 모두 HWT9037이 불리한 쪽이다.

3–5 Hz 대역에서 각 센서가 검출하려면 필요한 최소 외란:

| | mg | 각도 | % 기울기 |
| --- | --- | --- | --- |
| HWT9037 (2카운트 × 2.24배 감쇠) | 4.48 | 0.257° | 0.448 % |
| SCL3300 (잡음 3σ) | 0.45 | 0.026° | 0.045 % |

**HWT9037은 약 10배 큰 외란을 요구한다.** 4절의 책상 충격을 크기별로 줄여 보면 더 분명하다.

| 충격 크기 | HWT9037이 보는 카운트 | SCL3300이 보는 카운트 |
| --- | --- | --- |
| 실제 (21.4 mg) | 1 | 258 |
| 1/5 | 0.2 | 52 |
| 1/10 | 0.1 | 26 |
| 1/20 | 0 | 13 |

**그 충격의 1/5 크기 외란은 HWT9037에게 완전히 보이지 않고, SCL3300에게는 52카운트로
또렷하다.** 크레인의 삐걱거림이 이 영역에 들어온다면 두 센서는 교체 가능하지 않다.

> **이 프로젝트에 대한 함의**
>
> 이 장비의 판정 로직이 하는 일은 "크레인이 안정되었는가"를 묻는 것이다. 짧은 흔들림을
> 매끄럽게 지워버리는 출력은 그 질문에 **흔들리는 중에도 "안정"이라고 답할 수 있다.**
> 낮은 σ와 이 성질은 같은 것의 양면이라 하나만 골라 가질 수 없다.

한 가지 덧붙이면, HWT9037의 이 열세는 절반이 자초한 것이다. 드라이버의 `round(..., 3)`이
없다면 눈금이 1 mg가 아니라 0.488 mg여서 위 표의 HWT9037 항목이 2배 좋아진다 (11절).
그래도 SCL3300보다 5배 나쁘다.

---

## 3. 원시 σ 비교가 틀린 이유 — HWT9037의 출력은 이미 필터를 지났다

### 3.1 원시 수치만 보면 HWT9037의 압승

정지 상태에서 각자의 각도 출력을 그대로 놓고 표준편차를 재면 이렇다.

| 항목 | HWT9037-485 | SCL3300 |
| --- | --- | --- |
| AngX σ | **0.0005°** | 0.0097° |
| AngY σ | **0.0005°** | 0.0112° |
| AngX peak-to-peak | **0.0030°** | 0.0879° |
| AngY peak-to-peak | **0.0040°** | 0.0934° |

19배 차이. 여기서 멈추면 결론은 하나뿐이다. 그런데 같은 데이터에 이 결론과 양립할 수
없는 증거가 셋 있다.

### 3.2 증거 하나 — 잡음이 자기 눈금보다 325배 작다

정지한 센서의 가속도 벡터 크기 `|acc|`는 중력 그대로 1 g여야 하고, 그 흔들림은 순수한
측정 잡음이다.

| 항목 | HWT9037-485 | SCL3300 |
| --- | --- | --- |
| 분해능 | 0.488 mg/LSB | 0.083 mg/LSB |
| `\|acc\|` σ | 0.0015 mg | 0.1508 mg |
| 150초 peak-to-peak | 0.008 mg | 2.428 mg |
| **σ ÷ 자기 LSB** | **1/325** | **1.8** |

ADC 한 칸을 못 넘는 잡음은 필터 없이 존재할 수 없다. 150초 동안 HWT9037의 `|acc|`가
움직인 전체 폭이 자기 LSB의 1/61이라는 것은, 그 시간 내내 출력이 사실상 **상수**였다는
뜻이다. SCL3300의 1.8 LSB가 오히려 정상적인 미가공 출력의 모습이다.

### 3.3 증거 둘 — 평균을 내도 나아지지 않는다

진짜 백색잡음은 N개를 평균 내면 `1/√N`로 줄어든다. 이미 자기 바닥까지 평활된 출력은
연속 샘플이 서로 독립이 아니라서 아무리 평균 내도 꿈쩍하지 않는다.

각도 σ (도), 25 Hz 기준:

| 구간 N | 1 | 2 | 5 | 10 | 25 | 50 | 100 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 유효 대역폭 | 12 Hz | 6.2 Hz | 2.5 Hz | 1.2 Hz | 0.5 Hz | 0.25 Hz | 0.12 Hz |
| HWT9037 AngX | 0.0005 | 0.0005 | 0.0005 | 0.0005 | 0.0004 | 0.0004 | 0.0004 |
| — 이상적 1/√N | 0.0005 | 0.0004 | 0.0002 | 0.0002 | 0.0001 | 0.0001 | 0.0001 |
| SCL3300 AngX | 0.0098 | 0.0073 | 0.0033 | 0.0023 | 0.0014 | 0.0009 | **0.0006** |
| — 이상적 1/√N | 0.0098 | 0.0069 | 0.0044 | 0.0031 | 0.0020 | 0.0014 | 0.0010 |
| SCL3300 AngY | 0.0112 | 0.0093 | 0.0040 | 0.0025 | 0.0014 | 0.0009 | 0.0006 |

HWT9037은 100개를 평균 내도 자기 점선을 완전히 무시한 채 수평을 유지한다. SCL3300은
그 선을 따라 내려가 **N=100(0.12 Hz)에서 0.0006°로 HWT9037의 0.0004°와 만난다.** 이
지점에서 둘을 제한하는 것은 센서가 아니라 벤치에 남은 실제 미세 움직임이다.

### 3.4 증거 셋 — 그 σ는 센서가 아니라 방을 재고 있다

HWT9037의 σ는 고정된 값이 아니다. 사람이 오가던 다른 측정에서는 같은 축이 **0.0070°**로
나왔다 — 14배 차이. 그때도 N=1부터 100까지 완전히 평평했다.

즉 HWT9037의 σ가 재고 있는 것은 자기 잡음이 아니라 주변의 느린 실제 움직임이다. 자기
측정 잡음은 어느 대역에서도 보이지 않는다. 관측 가능한 범위 아래에 있기 때문이다.

> **따라서 σ 비교는 대역폭을 맞춘 뒤에만 유효하다.** 원시 σ를 나란히 놓는 순간 "어느
> 출력이 더 필터링되었는가"를 재게 되고, 그건 센서 품질과 아무 관계가 없다.

---

## 4. HWT9037의 두 한계 — 2–3 Hz 코너와 0.488 mg 양자화

3절은 필터가 있다는 것까지만 말한다. 그 필터가 무엇을 지우는지는 흔들어서 재야 한다.

### 4.1 주파수 응답 — 1.4 Hz까지는 충실하다

두 센서의 잡음 바닥을 모두 넘는 지속 가진(진폭 100–200 mg, 잡음 바닥의 40배)으로 120초를
녹화하고 4초 창으로 훑어 주파수별로 분류했다.

| 대역 | 창 수 | HWT9037 | SCL3300 | 비 (SCL/HWT) | 1차 가정 시 f<sub>c</sub> |
| --- | --- | --- | --- | --- | --- |
| 0.00 – 0.35 Hz | 24 | 203.7 mg | 209.8 mg | **1.03** | — |
| 0.35 – 0.70 Hz | 5 | 133.6 mg | 132.3 mg | **0.99** | — |
| 0.70 – 1.40 Hz | 4 | 113.8 mg | 119.1 mg | **1.05** | 3.22 Hz |
| 1.40 – 2.80 Hz | 3 | 103.5 mg | 122.9 mg | 1.19 | 3.09 Hz |
| 2.80 – 5.60 Hz | 9 | 19.1 mg | 42.6 mg | **2.24** | 1.98 Hz |

**HWT9037은 1.4 Hz까지 SCL3300을 5 % 이내로 따라온다.** 큰 신호를 저주파에서 충실히
추종한다는 뜻이다. 1.4 Hz 위에서 뒤처지기 시작해 2.8–5.6 Hz에서는 2.24배 감쇠한다.
1차 저역통과로 보면 **차단 주파수는 약 2–3 Hz**다.

### 4.2 충격 — 22배 감쇠, 그러나 원인은 진폭이 아니라 주파수

잡음 측정 중 누군가 책상을 건드렸다. 두 센서가 같은 순간에 5σ 이벤트를 잡았으니 실재한
사건이다. 각자의 각도 출력이 그것을 어떻게 보고했는가:

| t (s) | HWT9037 AngX | SCL3300 AngX |
| --- | --- | --- |
| 102.56 | 0.2250 | −0.3406 |
| 102.64 | 0.2250 | −0.0714 |
| 102.72 | 0.2250 | −0.3516 |
| 102.76 | 0.2220 | −0.4230 |
| 102.88 | 0.2230 | −0.3186 |

HWT9037은 **자기 가속도계가 충격을 겪는 동안 각도 출력이 0.004° 범위에만 머물렀다.**
SCL3300은 같은 사건에 0.35° 움직였다.

각도 range끼리의 이 비교는 SCL3300의 백색잡음을 함께 세므로 감쇠를 과장한다. 잡음이
아닌 신호로 재려면 가속도 벡터의 최대 편차를 쓰는 것이 옳다:

| 충격 구간 (95–112 s) | HWT9037-485 | SCL3300 |
| --- | --- | --- |
| `\|acc\|` 최대 편차 | 0.98 mg (**2 LSB**) | 21.4 mg (258 LSB) |
| 지배축 편차 σ | 0.56 mg | 1.98 mg |
| 감쇠비 (최대 편차) | — | **22배** |

4.1의 곡선이 이 22배를 설명한다. **감쇠의 원인은 진폭이 아니라 주파수다** — 순간 충격의
에너지는 대부분 5 Hz 위에 있어 그 곡선의 오른쪽 바깥에 있다.

### 4.3 필터의 시정수는 이 데이터로 구할 수 없다

SCL3300을 필터 걸리지 않은 기준 입력으로 놓고 `LPF(SCL, τ)`가 HWT9037을 재현하는 τ를
찾으면 그 시정수가 나와야 한다. 되지 않는다. 어떤 τ에서도 상관계수가 `|0.3|`을 넘지
못하고, 대역을 나눠 봐도 마찬가지다:

| 대역 | HWT9037 σ | SCL3300 σ | 상관 |
| --- | --- | --- | --- |
| 2.5 – 12.5 Hz | 0.335 mg | 0.421 mg | −0.019 |
| 0.5 – 2.5 Hz | 0.241 mg | 0.604 mg | −0.042 |
| 0.1 – 0.5 Hz | 0.094 mg | 0.181 mg | −0.072 |
| 0.02 – 0.1 Hz | 0.082 mg | 0.075 mg | +0.015 |

**HWT9037의 대역별 σ가 모두 자기 LSB 0.488 mg보다 작다.** 남아 있는 것은 신호가 아니라
양자화 디더이고, 그래서 사건의 형태가 출력에 남아 있지 않다. 감쇠가 22배라는 하한은
말할 수 있지만 시정수는 말할 수 없다 — 형태가 사라진 신호에는 시정수가 없다.

> **정리하면** HWT9037은 두 개의 서로 다른 한계를 갖는다. 큰 신호에는 약 2–3 Hz의 코너를
> 갖는 저역통과이고, 작은 신호에는 0.488 mg 양자화에 막혀 아예 아무것도 보고하지 않는다.
> 충격이 2 카운트만 남긴 것은 두 한계가 함께 작용한 결과다.

---

## 5. 분해능과 정밀도

둘은 다른 것이다. **분해능**은 표현할 수 있는 최소 눈금이고, **정밀도**는 같은 값을 다시
읽었을 때의 재현성이다. 분해능이 곱다고 정밀한 것도, 정밀하다고 분해능이 고운 것도 아니며
이 두 부품은 그 사실을 각각 반대 방향으로 보여준다.

### 5.1 실측 눈금

150초 기록에서 실제로 나타난 값들의 최소 간격이다. 사양이 아니라 데이터 경로 끝에서 나온
숫자다.

| 필드 | 실측 눈금 | 부품 사양 | 150초간 고유값 |
| --- | --- | --- | --- |
| HWT9037 AccX/Y | **1.0 mg** | 0.488 mg | **2개** |
| HWT9037 AccZ | — | 0.488 mg | **1개** |
| SCL3300 AccX/Y/Z | 0.08 mg | 0.083 mg | 20 – 25개 |
| HWT9037 AngX/Y | 0.001° | 0.001° | 4 – 5개 |
| SCL3300 AngX/Y | 0.0054° | 0.00549° | 17 – 18개 |

두 가지가 눈에 띈다.

**하나.** HWT9037의 실측 가속도 눈금이 부품 사양의 2배다. 드라이버의 반올림 때문이며
11절에 적었다.

**둘.** 각도 눈금은 순위가 뒤집혀 HWT9037이 5.4배 곱다. 그러나 HWT9037 가속도계의 1 LSB는
각도로 환산하면 **0.028°**다 (0.488 mg ÷ 1 g, 라디안). 즉 0.001°는 센서가 실제로 분해하는
값이 아니라 **내부 필터가 보간해낸 숫자**다. 곱게 보고한다고 그만큼 분해하는 것은 아니다.

### 5.2 정밀도는 대역폭을 정하기 전에는 정의되지 않는다

| 대역폭 | HWT9037 σ | SCL3300 σ |
| --- | --- | --- |
| 12.5 Hz (25 Hz 원시) | **0.0005°** | 0.0097° |
| 1.2 Hz (25샘플 중 10 평균) | 0.0005° | 0.0023° |
| 0.5 Hz (25 평균) | 0.0004° | 0.0014° |
| 0.12 Hz (100 평균) | 0.0004° | **0.0006°** |

HWT9037은 어느 대역에서든 0.0005°에 붙박여 있고, SCL3300은 계속 좋아져 0.12 Hz에서 만난다.

주목할 점은 SCL3300의 0.0006°가 **자기 보고 눈금 0.0054°보다 9배 작다**는 것이다. 잡음이
디더 역할을 해 양자화 아래로 내려가는, 오버샘플링의 정상적인 결과다. HWT9037이 0.001° 눈금
위에서 σ 0.0005°를 내는 것도 같은 원리이지만, 그쪽은 **부품 안에서 이미 끝나 있어 이쪽이
정도를 고를 수 없다.**

---

## 6. 필요한 샘플링 속도 — 두 센서가 정반대다

**25 Hz는 HWT9037에는 과잉이고 SCL3300에는 최적이다.** 두 부품의 요구가 다르기 때문이며,
근거는 아래 두 가지다.

### 6.1 HWT9037 — 1 Hz로 떨어뜨려도 잃을 것이 없다

3.3절의 결과에는 직접적인 따름정리가 있다. 평균이 듣지 않는 출력은 연속 샘플이 서로
독립이 아니라는 뜻이고, 독립이 아닌 샘플을 빠르게 받는 것은 같은 숫자를 다시 받는 일이다.

자기상관계수:

| 시차 | HWT9037 AngX | SCL3300 AngX |
| --- | --- | --- |
| 1 샘플 (0.04 s) | **+0.872** | +0.115 |
| 25 샘플 (1.0 s) | +0.395 | +0.117 |
| 125 샘플 (5.0 s) | +0.248 | +0.025 |
| 250 샘플 (10 s) | +0.100 | +0.030 |

HWT9037은 바로 옆 샘플과 87 % 같은 값이다. SCL3300은 12 %로, 매 샘플이 새 정보다.

25 Hz 원본을 솎아내고 직전 값을 유지해 재구성했을 때의 오차:

| 샘플링 | HWT9037 RMS | HWT9037 최대 | SCL3300 RMS |
| --- | --- | --- | --- |
| 12.5 Hz | 0.00020° | 0.001° | 0.0091° |
| 5 Hz | 0.00033° | 0.001° | 0.0130° |
| 1 Hz | 0.00047° | **0.001°** | 0.0127° |
| 0.2 Hz | 0.00065° | 0.002° | 0.0136° |

**HWT9037은 1 Hz로 떨어뜨려도 최대 오차가 0.001°** — 그 출력의 양자화 한 칸이다. 책상
충격이 있던 구간에서도 1 Hz 재구성 오차는 RMS 0.001° / 최대 0.007°에 그쳤다. SCL3300은
반대로 12.5 Hz에서 이미 0.009°가 벌어지는데, 이는 `√2 × σ`와 일치하며 정보 손실이 아니라
매 샘플이 독립이라는 증거다.

**단, 느리게 받는다고 잃은 것이 돌아오지는 않는다.** 빠른 사건을 보려면 샘플링 속도가
아니라 센서를 바꿔야 한다. 4절이 그 이야기다.

### 6.2 SCL3300 — 25 Hz가 정확히 맞고, 낮추려면 솎지 말고 평균해야 한다

SCL3300은 모드 3에서 내부 LPF가 10 Hz이고, 출력 레이트를 내부에서 낮추는 기능은 없다.
모드가 바꾸는 것은 LPF뿐이며 10 Hz가 가장 좁다. **따라서 느린 주기로 띄엄띄엄 읽으면
0.5 – 10 Hz의 잡음이 그대로 접혀 들어온다.**

같은 출력 레이트를 만드는 두 방법의 차이:

| 출력 레이트 | 솎아내기 σ | 평균 σ | 이득 |
| --- | --- | --- | --- |
| 5 Hz | 0.00993° | 0.00325° | 3.1배 |
| 2.5 Hz | 0.00935° | 0.00229° | 4.1배 |
| 1 Hz | 0.00854° | 0.00137° | 6.2배 |
| 0.5 Hz | 0.00880° | 0.00087° | 10.2배 |
| 0.2 Hz | 0.00810° | 0.00062° | **13.1배** |

솎아내기는 출력을 아무리 낮춰도 σ가 0.009°에서 내려오지 않는다. 접혀 들어온 잡음이 그대로
남기 때문이다. N개 평균은 그 자체가 boxcar 안티앨리어싱 필터이므로 `1/√N`으로 줄어든다.

읽기 속도가 25 Hz여야 하는 이유도 같다. 내부 LPF 10 Hz에 대해 Nyquist가 12.5 Hz이고,
자기상관 lag 1이 `+0.115`로 이웃 샘플이 거의 독립이다. 독립이어야 평균이 `1/√N`으로 듣는다.
더 빠르게 읽으면 샘플이 상관되어 이득이 줄고, 더 느리게 읽으면 접힌다.

권장 구성:

```
SCL3300을 25 Hz로 읽는다
  → 기록은 25 Hz 원본 그대로
  → 판정용은 N개 평균으로 원하는 대역을 만든다
       N = 25  → 0.5 Hz,  σ 0.0014°
       N = 100 → 0.12 Hz, σ 0.0006°
```

느린 출력이 필요해도 **읽기는 25 Hz를 유지해야 한다.** 느린 것은 평균으로 만드는 것이지
느리게 읽어서 얻는 것이 아니다.

---

## 7. 절대 정확도와 스케일

### 7.1 절대 정확도 — 여기서는 HWT9037이 우세

중력은 눈금이 필요 없는 유일한 기준이다. 정지한 센서는 방향과 무관하게 `|acc| = 1.000 g`를
읽어야 하고, 벗어난 만큼이 감도·오프셋 오차다.

| 항목 | HWT9037-485 | SCL3300 |
| --- | --- | --- |
| `\|acc\|` 평균 | 1.00102 g | 0.99586 g |
| 1 g 대비 오차 | **+0.10 %** | −0.41 % |
| AngX 드리프트 | +0.00006 °/분 | +0.00018 °/분 |
| AngY 드리프트 | +0.00018 °/분 | +0.00038 °/분 |
| 온도 (동시) | 27.17 °C | 25.67 °C |

150초 드리프트는 양쪽 모두 무시 가능하다.

다만 이 오차의 무게를 과대평가하면 안 된다. 각도는 축들의 **비**에서 나오므로 세 축에
공통으로 걸린 감도 오차는 각도 계산에서 상쇄된다. 축별 오프셋만 남는데, 둘을 갈라내려면
텀블 테스트가 필요하고 아직 하지 않았다. 그리고 잡음과 달리 감도 오차는 **교정으로 없앨
수 있다.**

### 7.2 스케일 — 차이가 검출되지 않는다

두 센서를 아크릴 판에 함께 고정하고, 안정된 두 자세를 각각 750샘플씩 평균 내어 그
사이의 회전을 쟀다. 각 센서는 자기 가속도 벡터가 두 자세 사이에 돌아간 각도를 보고한다.
이것은 **회전의 크기**이고, 회전 크기는 두 부품이 판 위에 어떤 방향으로 붙어 있든 같은
물리량이다. 그래서 축 매핑 없이 비교가 성립한다.

| 자세 쌍 | HWT9037 | SCL3300 | 차이 | 비 |
| --- | --- | --- | --- | --- |
| level → ref | 12.473° | 12.741° | +0.268° | +2.15 % |
| tilt-tail → ref | 12.460° | 12.373° | −0.087° | −0.70 % |
| poseB → ref | 13.895° | 13.941° | +0.046° | +0.33 % |
| | | | **평균** | **+0.6 % ± 1.4 %** |

**스케일 차이는 검출되지 않았다.** 2 % 이상의 차이가 있었다면 여기 숨을 수 없다.

### 7.3 이 측정의 한계 — 그리고 그것이 총 오차의 1위인 이유

세 값이 서로 2.9 %나 어긋난다는 사실 자체가 답이다. 같은 물리적 회전을 잰 독립 측정끼리의
불일치가 곧 이 측정의 불확실도이고, 그 한계는 센서가 아니라 **자세를 다시 잡는 일**에서
온다. 각도로는 0.25°이며, 이것이 2.1절 표의 최대 항목이다.

첫 시도는 1.8°만 움직였고 SCL3300이 −7.65 % 작다고 보고했다. 그 숫자는 자기 자신을
지탱하지 못한다. 스케일은 비율이라 기준 자세의 오차가 그대로 나눠 들어가고, 1.8°에서는
센서가 개입하기도 전에 ±14 %가 깔린다. 도구는 이제 그런 각도에서 스스로 경고한다.

더 좁히려면 각도를 키우면 된다. 같은 기준 오차가 12°에서는 ±2 %지만 45°에서는 ±0.6 %로
줄어든다. 다만 이 프로젝트가 재는 크레인 기울기 범위를 생각하면 **2 % 이내 일치는 이미
필요한 것보다 엄격하다.**

---

## 8. 공유 SPI 버스는 영향이 없다

e-paper 패널이 CE0를 쓰는 상태에서 SCL3300을 CE1에 붙였다. 별도 확인 결과:

| 항목 | SCL3300 단독 | e-paper 리프레시 동시 |
| --- | --- | --- |
| 샘플 | 500 / 5 s | 2,000 / 20 s |
| 실측 레이트 | 100.0 Hz | 100.0 Hz |
| 최악 루프 지연 | 10.1 ms | 11.5 ms |
| CRC 오류 | 0 | 0 |

패널이 흰색·검정·패턴 3회 전체 리프레시를 도는 동안 간섭은 1.5 ms뿐이었다. 1.4초짜리
리프레시 중 실제로 버스를 점유하는 구간이 10 ms 남짓이고 나머지는 BUSY 폴링이라 버스가
놀기 때문이다. 패널 쪽 전송 청크 조정은 불필요하다.

경합이 없는 구조적 이유는 세 가지다. 패널은 write-only라 MISO를 아예 쓰지 않아
SCL3300이 GPIO9를 독점하고, 두 부품 모두 SPI 모드 0이며, 커널의 컨트롤러 락이 전송을
직렬화한다. 자세한 것은 [scl3300.py](scl3300.py) 헤더 주석에 있다.

---

## 9. 방법론 — 왜 이 세 지표인가

두 센서의 각도 출력은 같은 것을 뜻하지 않는다. HWT9037은 융합된 오일러 각을, SCL3300은
각 축의 수평면 대비 경사를 낸다. **하나에서 다른 하나를 빼면 센서가 아니라 장착 상태를
재게 된다.** 그래서 장착 방향을 몰라도 성립하는 세 가지에 기댔다.

| 지표 | 무엇을 재는가 | 왜 장착과 무관한가 |
| --- | --- | --- |
| `\|acc\|` 대 중력 | 감도·오프셋 오차 | 정지 시 방향과 무관하게 1 g |
| 정지 시 σ | 반복도 | 외부 기준이 필요 없음 |
| 기준 자세로부터의 회전 | 스케일 | 회전 크기는 두 프레임에서 같은 값 |

여기에 3.4절의 단서를 더해야 한다 — **σ 비교는 대역폭을 맞춘 뒤에만 유효하다.**

---

## 10. 재현 방법

```bash
# 잡음·정확도 (레코더를 먼저 내려야 포트가 빈다)
sudo systemctl stop verticrane-recorder
.venv/bin/python dev/sensor_compare.py --seconds 150 --rate 25 --mode 3 --csv /tmp/base.csv

# 스케일: 자세마다 한 번씩 찍고, 센서 없이 두 자세를 비교
.venv/bin/python dev/sensor_compare.py --seconds 30 --csv /tmp/level.csv
#   ... 판을 10-20도 기울여 고정 ...
.venv/bin/python dev/sensor_compare.py --seconds 30 --csv /tmp/tilted.csv
.venv/bin/python dev/sensor_compare.py --compare /tmp/level.csv /tmp/tilted.csv

# SCL3300 단독 확인
.venv/bin/python dev/scl3300_test.py --probe
```

### 주파수 응답 측정

```bash
# 속도를 바꿔가며 계속 흔들면서 한 번만 녹화하면 된다
.venv/bin/python dev/sensor_compare.py --seconds 120 --rate 25 --csv /tmp/sweep.csv
.venv/bin/python dev/sensor_compare.py --sweep /tmp/sweep.csv
```

`--sweep`은 4초 창을 미끄러뜨리며 각 창의 주파수를 교차율로 읽고 대역별로 분류한다. 흔드는
사람이 시계를 볼 필요가 없고, 한 파일에 두 주파수가 섞이는 사고도 없다.

**진폭이 결정적이다.** HWT9037의 LSB는 0.488 mg이고, 그 10칸(4.9 mg)에 못 미치는 창은
응답이 아니라 양자화 격자를 재게 되므로 분석이 버리고 개수만 센다. 위 측정에서 쓸 만했던
진폭은 100–200 mg였다.

정해진 리듬으로 세 번 나눠 찍고 싶다면 `dev/shake_run.py`가 카운트다운과 함께 안내한다.

관련 파일: [dev/sensor_compare.py](dev/sensor_compare.py) · [scl3300.py](scl3300.py) ·
[dev/scl3300_test.py](dev/scl3300_test.py) · [test_scl3300.py](test_scl3300.py)

---

## 11. 기존 코드에 대한 관찰

측정 도중 드러난 것들이다. **이 보고서는 관찰만 기록하며 기존 코드는 변경하지 않았다.**

| 위치 | 관찰 | 영향 |
| --- | --- | --- |
| [hwt9037_485.py](hwt9037_485.py) `round(..., 3)` | 가속도를 1 mg로 반올림해 부품의 0.488 mg 분해능 중 절반을 데이터 경로에서 버린다. AccZ는 150초 동안 값이 하나뿐이었다 (5.1절) | 2.3절의 검출 한계가 2배 나빠진다 |
| [recorder.py](recorder.py)의 25 Hz 근거 | 주석의 근거는 "센서 대역폭이 10 Hz이므로 Nyquist"인데, 대역폭 레지스터는 원시 경로에 걸리고 **융합된 각도 출력의 실효 대역폭은 1 Hz에도 못 미친다** (6.1절) | 근거가 각도 경로에는 성립하지 않는다. 다만 SCL3300을 쓴다면 25 Hz는 별개의 이유로 옳다 (6.2절) |
| [stability.py](stability.py)의 5초 창 | 창 안 σ와 창 평균들의 σ 비율로 유효 독립 샘플 수를 추정하면 HWT9037은 125샘플 중 **약 2.5개**, SCL3300은 약 100개 | 창 안에서 계산하는 분산·표준편차의 자유도가 겉보기와 크게 다르다 |

---

## 12. 남은 결정 — 대체인가 병행인가

비교 자체는 끝났다. 남은 것은 채택 방식이고, 갈림길은 **SCL3300에 자이로가 없다**는
점에서 갈린다.

| | 대체 | 병행 |
| --- | --- | --- |
| 변경 범위 | [recorder.py](recorder.py)의 샘플 소스 | 레코드 포맷 또는 파일 분리 |
| 걸리는 곳 | [ahrs_file.py](ahrs_file.py) 128바이트 레코드의 자이로 필드가 빈다 | 기존 `.dat` 파일과의 호환 |
| 확인 필요 | [stability.py](stability.py)의 정지 판정이 자이로를 쓰는지 | 두 센서 동시 운용 시 전력·CPU |

어느 쪽이든 2.3절의 함의가 판단 기준에 들어가야 한다. 잡음만 보면 HWT9037이 매력적이지만,
그 잡음이 낮은 이유와 짧은 흔들림을 못 보는 이유가 같은 것이다.
