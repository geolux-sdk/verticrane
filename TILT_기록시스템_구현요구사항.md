# TILT METER 자동 기록 시스템 구현 요구사항

Verticrane 기울기 측정 장비(Raspberry Pi Zero 2 W + HWT9037-485)를 **운영자가
브라우저만으로 쓸 수 있는 무인 기록 장치**로 만드는 작업의 요구사항이다.

기존 저장소에 이미 구현된 센서 통신·설정·분석 코드는 **개발자 전용 도구**로 남기고,
이번에 새로 만드는 자동 기록기와 운영자 웹만 상시 동작한다.

---

## 1. 전제 조건

이번 작업은 백지에서 시작하지 않는다. 아래는 이미 동작이 확인된 것들이며 **그대로 재사용한다.**

| 재사용 대상 | 파일 | 비고 |
| --- | --- | --- |
| 센서 통신·레지스터 파싱 | `hwt9037_485.py` | Modbus RTU, 임의 변경 금지 |
| 시리얼 포트 결정 | `port_config.py` | `--port` / 환경변수 / 자동감지 |
| 설정 저장·PIN·로그 설정 | `app_config.py`, `config.json` | 여기에 기록기 설정을 추가 |
| 기울기(경사도) 계산 | `log_tilt.resultant_slope_pct()` | Roll/Pitch → % |
| e-paper 패널 구동 | `gdey0154d67.py`, `eink_status.py` | 렌더링 로직 재사용 |

### 센서는 스트림이 아니라 폴링이다 (중요)

HWT9037-485는 데이터를 자발적으로 흘려보내지 않는다. 마스터가 Modbus RTU로
`readReg(0x34, 15)`를 호출해야 한 세트를 돌려준다. 따라서:

- "원본 Serial payload를 그대로 저장" 같은 개념은 존재하지 않는다. **디코드된 값을 저장한다.**
- 샘플 주기는 우리가 정한다. 센서 대역폭이 10 Hz이므로 Nyquist(20 Hz)에 여유를 둔
  **25 Hz**를 유지한다.
- 온도(`0x43`)는 느리게 변하므로 1초에 한 번만 갱신한다.

### 현장 네트워크 조건

네트워크는 **장비를 싣고 다니는 차량이 제공하는 WiFi**다. 이 사실이 설계 곳곳을
좌우하므로 먼저 못 박아 둔다.

| 상황 | WiFi | 실제로 일어나는 일 |
| --- | --- | --- |
| 차량 근처 (준비·회수) | 있음 | 운영자가 접속해 설정하고 파일을 받는다 |
| 측정 지점으로 이동 | 끊김 | 거리가 멀어지면 연결이 떨어진다 |
| **멀리서 전원을 켰을 때** | **없음** | **부팅 시점에 네트워크가 아예 없다** |
| 차량이 가까워질 때 | 다시 붙음 | 도중에 연결이 살아날 수 있다 |

**네트워크와 운영자 접속은 같은 조건이다.** 차량 WiFi가 닿으면 운영자 브라우저도
닿고 NTP도 된다. 안 닿으면 둘 다 안 된다. 따라서:

1. **부팅 시 NTP가 될 거라 기대하면 안 된다.** 하필 기록이 가장 중요한 순간
   (장비를 들고 나가 켜는 순간)에 네트워크가 없다. 시각 확보는 부팅 때 한 번
   시도하고 끝내는 절차가 아니라, **동작 내내 계속 시도하다가 확보되면 소급
   적용하는 절차**여야 한다(§3).
2. **운영자가 브라우저로 시각을 맞춰 주는 기능은 필요 없다.** 그 화면에 접속할 수
   있는 상황이면 NTP가 이미 시각을 맞춰 놓았다. 사람 손을 빌릴 이유가 없다.
3. **§3의 60초 HTTP 규칙이 이 지형과 정확히 맞아떨어진다.** 차량 근처에서 켜면
   운영자가 접속해 `maintenance`로 가고, 멀리서 켜면 접속할 사람이 아예 없으므로
   자동으로 기록이 시작된다. 운영자가 따로 모드를 고를 필요가 없다.

### 설정 저장소는 `config.json` 하나로 통일

`.env`를 새로 도입하지 않는다. 이미 `app_config.py`가 `config.json`을 관리하고
있고(임계값, 로그 레벨, PIN 해시), 여기에 `recorder` 섹션을 추가한다. 설정 파일이
두 개가 되면 "어느 쪽이 적용된 값인가"를 계속 헷갈리게 된다.

데이터 경로도 새 경로를 만들지 않고 기존 `data/`(이미 gitignore 처리됨)를 쓴다.

---

## 2. 시스템 구성

프로세스는 **하나**다. 기록 루프와 Flask 웹서버를 한 프로세스 안의 두 스레드로 돌린다.
프로세스가 둘이면 시리얼 포트를 두고 다투게 되고, 상태 공유를 위해 IPC가 필요해진다.

```
verticrane-recorder.service   (systemd, 부팅 시 자동 시작)
└─ recorder.py
   ├─ 기록 스레드 : 센서 25 Hz 폴링 → 상태 판정 → 바이너리 기록
   ├─ 웹 스레드   : Flask, 0.0.0.0:8080 (상태/파일/설정)
   └─ 패널 스레드 : e-paper 주기 갱신
```

기존 Streamlit 대시보드 서비스(`verticrane-dashboard.service`)는 **자동 시작에서 제외한다.**
Zero 2 W에서 Streamlit을 상시 띄워 두면 CPU와 메모리를 계속 먹는다.

---

## 3. 동작 상태와 부팅 절차

상태는 넷이다.

| 상태 | 의미 |
| --- | --- |
| `waiting_http` | 부팅 직후. HTTP 접속을 기다린다 |
| `waiting_stable` | 접속이 없어 자동 기록으로 가는 중. 센서 안정화를 기다린다 |
| `recording` | 안정화 확인 후 기록 중 |
| `maintenance` | 대기 시간 안에 접속이 있었다. 자동 기록하지 않는다 |

절차:

1. 부팅 즉시 HTTP 서버를 띄우고, 남아 있는 `.partial` 파일을 복구한다(§6).
2. 시각을 확보한다(아래). **확보 못 해도 기록을 미루지 않는다.** 나중에 소급 보정한다.
3. **설치 유예 시간**(`mount_delay_seconds`, 기본 60초)을 **무조건 기다린다.**
   네트워크와 무관하다.
4. 네트워크가 붙어 있으면 주소가 나올 때까지 기다린 뒤(`network_wait_seconds`),
   **주소가 나온 시점부터** `http_wait_seconds`(기본 60초) 동안 요청을 기다린다.
   붙어 있지 않으면 이 단계를 건너뛴다.
5. **요청이 있으면** → `maintenance`. 자동 기록을 시작하지 않는다.
   운영자가 파일을 받으러 온 상황이므로 SD 카드 쓰기를 늘리지 않는다.
   이미 기록 중이었다면 그 기록을 버린다(아래).
   웹에서 "지금 기록 시작" 버튼을 누르면 수동으로 `waiting_stable`로 넘어갈 수 있다.
6. **요청이 없으면** → `waiting_stable`. 센서를 계속 폴링하며 안정화를 판정한다.
7. 안정화 기준을 만족하면 → `recording`. 파일 하나를 만들어 기록을 시작한다.
8. 전원이 꺼지면 기록이 끊긴다. 다음 부팅의 1단계에서 복구된다.

### 시각 확보와 소급 보정

Zero 2 W에는 RTC가 없다. 부팅 직후의 시각은 1970년처럼 대놓고 틀린 게 아니라
**그럴듯하게 틀려 있다** — 그대로 파일명을 지으면 나중에 아무도 이상한 줄 모른다.

§1의 네트워크 조건 때문에 **부팅 시점의 NTP는 실패하는 쪽이 기본**이다. 시각 확보를
"기록 시작 전에 끝내야 하는 관문"으로 두면 네트워크 없는 현장에서 영원히 기록을
못 시작한다. 그래서 뒤집는다.

> **일단 기록을 시작하고, 시각은 확보되는 대로 소급 적용한다.**

블록의 시간축이 단조 시계 기준이므로 가능하다. 파일 하나에 필요한 절대 시각은
**기준점 하나**뿐이고, 그 기준점은 나중에 알아내도 늦지 않다.

| 순위 | 경로 | 시각 품질 | 현재 상태 |
| --- | --- | --- | --- |
| 1 | RTC 모듈에서 읽기 | `rtc` | **미장착 — 자리만 확보** (§14) |
| 2 | NTP 동기화 | `synced` | 차량 WiFi가 닿을 때 |
| 3 | 마지막으로 알던 시각 복원 | `unsynced` | 실제보다 **뒤처짐** |
| 4 | 그마저 `min_valid_year` 이전 | `invalid` | 완전히 못 믿음 |

#### 1. 마지막으로 알던 시각 이어가기

`time_save_interval_seconds`(기본 10초)마다 현재 시각을 `data/.lastknown`에 원자적으로
저장하고, 부팅 시 NTP가 아직이면 이 값으로 시작한다.

`fake-hwclock`을 쓰지 않는 이유는 저장 주기가 **한 시간**이라서다. 10초마다 저장하면
오차의 대부분이 "전원 끄고 이동한 시간"으로 줄어든다.

복원한 시계는 **항상 실제보다 뒤처지고 절대 앞서지 않는다.** 기록된 시각이 미래일 수는
없다는 뜻이라, 나중에 판단할 때 쓸 만한 성질이다.

#### 2. 백그라운드 시각 감시

부팅 때 한 번이 아니라 **동작하는 내내** `ntp_retry_interval_seconds`(기본 30초)마다
확인한다. 차량이 가까워져 WiFi가 붙는 순간을 놓치지 않기 위해서다.
처음 `unsynced` → `synced`로 바뀌는 순간만 소급 적용 대상이고, 그 뒤의 미세 보정은 무시한다.

#### 3. 소급 보정 — 이름은 다음 부팅에 고친다

보정 결과를 **파일 안에 넣지 않는다.** 이미 쓴 데이터를 건드리지 않는 가장 확실한
방법은 아예 손대지 않는 것이고, 최종적으로는 **파일 이름**이 정확한 시각을 담는다.

**기록 중에는 이름을 바꾸지 않는다.**

- 이름을 바꾸는 것은 NTP 감시 스레드, 파일을 쓰는 것은 기록 스레드다. 현재 파일명을
  공유하므로 락 없이는 경쟁 상태가 된다.
- 개발 중 Windows에서는 열린 파일의 rename이 아예 실패한다.

**"기록 종료 시점"에 기대서도 안 된다.** 현장에서 기록을 멈추는 방법은 전원을 내리는
것뿐이고, 그때 정상 종료 절차를 밟을 기회는 없다.

그래서 기록 중에는 **`.timeinfo`만 갱신하고**(별개 파일이라 충돌 여지가 없다), 이름을
바로잡는 일은 전부 **다음 부팅의 복구 단계**에서 한다(§6). 그때는 아무것도 기록 중이
아니므로 충돌이 원천적으로 없다.

```text
부팅 N    기록 시작   TOP_b0007_20260827_143012.unsynced.ahrsbin.partial
                      TOP_b0007_20260827_143012.unsynced.ahrsbin.timeinfo
          NTP 확보    .timeinfo 만 갱신 (기록 중인 파일은 손대지 않는다)
          전원 차단   이름을 못 고친 채로 끝난다 — 정상이다
부팅 N+1  복구 단계   .partial + .timeinfo 를 읽어
                      → 마지막 온전한 블록까지 truncate
                      → 보정된 시각으로 이름 확정
                      TOP_20260827_144235.recovered.ahrsbin
```

**목록에 뜨는 파일은 언제나 이름이 바로잡힌 뒤**다. §3 절차 1번이 HTTP 대기보다 먼저
실행되므로, 운영자가 받는 시점에는 복구가 끝나 있다. 그래서 다운로드는
`Content-Disposition`에 그 이름을 그대로 실으면 된다.

NTP가 끝내 안 붙으면 이름은 `.unsynced`인 채로 남는다. 데이터는 온전하고, 운영자는
목록에서 그 표시를 보고 시각을 믿을 수 없음을 안다.

#### 4. 시각 정보 파일

기록을 시작할 때 함께 만들고, 보정이 일어나면 갱신한다.

```json
{
  "device_start_epoch": 1772179369.29,
  "corrected_start_epoch": 1772180112.48,
  "offset_seconds": 743.19,
  "quality": "synced",
  "source": "ntp",
  "applied_at_elapsed_ms": 812400,
  "original_filename": "TOP_b0007_20260827_143012.unsynced.ahrsbin",
  "boot_count": 7
}
```

- **이 파일이 없어도 아무것도 잃지 않는다.** 정확한 시각은 이미 파일명에 있다.
  언제 얼마나 보정됐는지를 남기는 감사 기록이다.
- 원자적으로 쓴다(임시 파일 → `fsync` → rename). 다운로드 대상이 아니다.

#### 5. 그 밖에

- 시각 품질은 파일명(`.unsynced` 유무)과 `.timeinfo`에 담기고, `/api/status`와
  e-paper에 노출한다. **헤더에는 두지 않는다**(§5.2).
- 커널 시각이 `min_valid_year`(기본 2026년)보다 이전이면 품질을 `invalid`로 표시한다.
- **시각을 못 맞췄다고 기록을 미루거나 멈추지 않는다.** 어떤 경우에도 데이터가 우선이다.

### 왜 두 번 기다리는가

기다리는 이유가 둘이고, 섞으면 둘 다 망가진다.

| 대기 | 언제 | 이유 |
| --- | --- | --- |
| **설치 유예** | **항상** | 운영자가 아래에서 전원을 켜고 **타워크레인에 올라가서** 장비를 단다. 올라가는 동안 기록하면 등반이 파일 앞머리에 들어간다 |
| **접속 대기** | 네트워크가 있을 때만 | 옆에 있는 운영자에게 기록 시작 전에 접속할 기회를 준다 |

설치 유예를 네트워크 유무로 건너뛰면 안 된다. **네트워크가 없는 상황이 바로 크레인에
올라가는 상황**이기 때문이다.

반대로 접속 대기는 네트워크가 없으면 의미가 없다. 아무도 접속할 수 없는데 기다리는 것은
그냥 기록을 늦추는 것뿐이다.

접속 대기는 **주소가 나온 시점부터** 센다. 부팅 시점부터 세면 WiFi가 올라오는 동안
창이 지나가 버려서, 운영자가 접속하려 할 때는 이미 늦다.

두 대기 모두 **센서를 계속 폴링하면서** 기다린다. 그래야 대기가 끝나는 순간 안정화 창이
이미 차 있어 곧바로 판정할 수 있다. 그리고 두 대기 다 유효한 요청이 오면 즉시 끝난다.

### 접속하면 기록을 버린다

기록 중에 유효한 HTTP 요청이 오면 **그 기록을 휴지통으로 보내고** `maintenance`로 간다.

장비에 닿을 수 있다는 것은 그 옆에 사람이 있다는 뜻이고, 그건 차량이지 크레인이 아니다.
그 기록은 **부팅 대기 시간 안에 아무도 접속하지 않아서 시작된 것**이며, 책상 위에 놓인
상자를 재고 있었을 뿐이다. §3이 "운영자가 파일을 받으러 온 상황이므로 SD 카드 쓰기를
늘리지 않는다"고 한 것과 같은 논리다.

- **지우지 않고 휴지통에 넣는다.** 판단의 대상은 "운영자 목록에 무엇이 보일 것인가"이지,
  틀렸을 때 데이터가 사라져도 되는 종류의 판단이 아니다. 먼저 정상적으로 확정한 뒤
  옮기므로 휴지통에 있는 것도 읽을 수 있는 온전한 파일이다.
- **상태 페이지의 자기 갱신은 접속으로 세지 않는다.** 폴링에 `?auto=1`이 붙고 서버는
  이를 무시한다. 그렇게 하지 않으면 **차량에 브라우저를 열어 둔 채 크레인 근처로 가는
  순간 진짜 측정이 버려진다.**

### HTTP 대기 규칙의 범위

"60초 규칙"이 막는 것은 **자동 기록 시작뿐이다.** 파일 목록 조회와 다운로드는 어느
상태에서든 항상 동작한다. 기록 중에 운영자가 접속했다고 기록이 끊기면 안 된다.

"유효한 요청"이란 `/api/*` 또는 설정 화면에 대한 요청을 말한다. 헬스체크나
`favicon.ico` 같은 부수 요청은 접속으로 치지 않는다.

---

## 4. 안정화 판정

크레인이 흔들리는 동안의 데이터는 기준값으로 쓸 수 없으므로, 정지 상태를 확인한
뒤에 기록을 시작한다. **안정화 전 데이터는 어떤 파일에도 남기지 않는다.**

최근 `stability_window_seconds` 구간에서 아래를 모두 만족해야 한다.

| 지표 | 기준 설정 | 기본값 |
| --- | --- | --- |
| 자이로 3축 RMS | `gyro_rms_max_dps` | 0.5 °/s |
| 가속도 3축 표준편차 | `accel_std_max_g` | 0.01 g |
| Roll/Pitch/Yaw 표준편차 | `attitude_std_max_deg` | 0.3 ° |
| 구간 내 최소 샘플 수 | `stability_min_samples` | 100 |

- Yaw는 0/360도 경계에서 튀므로 **원형 평균(circular mean)** 으로 계산한다.
- 판정 함수는 별도 모듈로 분리해 기준을 바꾸거나 지표를 추가할 수 있게 한다.
- 마지막 판정 결과(각 지표의 실측값과 통과 여부)는 `/api/status`에 그대로 노출한다.
  운영자가 "왜 기록이 시작 안 되는지"를 물어볼 때 답할 수 있어야 한다.
- 기록 중 불안정 상태가 지속될 때의 처리는 `stop_on_unstable`로 정한다. 기본은
  `false` — **흔들림 자체가 기록해야 할 현상이므로 멈추지 않는 쪽이 맞다.**

---

## 5. 기록 파일 포맷

### 5.1 파일명

파일명은 **어느 장비의 것인지**로 시작한다.

```text
FLAG_[bNNNN_]YYYYMMDD_HHMMSS[.unsynced][.recovered].ahrsbin
```

| 예 | 뜻 |
| --- | --- |
| `TOP_20260827_143012.ahrsbin` | 확정된 파일 |
| `TOP_20260827_143012.ahrsbin.partial` | 기록 중 |
| `TOP_20260827_143012.recovered.ahrsbin` | 전원 차단 후 복구됨 |
| `TOP_b0007_20260827_143012.unsynced.ahrsbin` | 시각을 못 믿는 상태 |
| `UNSET_20260827_143012.ahrsbin` | **SENSOR_FLAG가 설정되지 않았다** |

#### FLAG를 맨 앞에 두는 이유

한 크레인에 BASE·MIDDLE·TOP 세 대가 붙는다. 셋이 같은 시각에 기록하면 파일명이
**전부 똑같아지고**, 운영자가 노트북에 받는 순간 서로 덮어쓴다. 세 파일을 놓고 볼 때
가장 먼저 구분해야 하는 것이 위치이므로 FLAG가 맨 앞에 온다.

설정되지 않았으면 `UNSET_`이 붙는다. `.unsynced`와 같은 원리로, **설정 실수가
파일명에 드러나게** 하는 것이다. 나중에 어느 높이의 데이터인지 모르는 파일이
생기는 것보다, 받는 순간 이상하다는 걸 알아채는 편이 낫다.

**부팅 카운터**는 시각을 믿을 수 없을 때만 붙는다. 이유는 두 가지다.

- **이름 충돌.** `fake-hwclock`은 부팅할 때마다 같은 값을 복원할 수 있다. 전원을
  넣었다 금방 끄기를 반복하면 서로 다른 기록이 같은 파일명을 갖게 된다.
- **순서.** 시계를 못 믿는 상황에서 기록의 선후를 알 수 있는 유일한 정보다.
  카운터를 앞에 두었으므로 파일 목록을 이름순으로 정렬하면 부팅 순서대로 나온다.

카운터는 `data/.bootcount`에 저장하고 부팅할 때마다 1 증가시킨다.

**카운터로도 겹칠 수 있다.** 한 번의 부팅 안에서 기록을 두 번 하면 카운터가 그대로고,
시각까지 못 믿는 상태면 초 단위 이름이 같아질 수 있다. 그때는 확장자 앞에 `_2`,
`_3`을 붙여 유일하게 만든다.

> **기존 기록을 덮어쓰는 일은 어떤 경우에도 없다.** 이름이 겹치면 새 파일이
> 비켜선다. 이미 받아 둔 측정이 새 기록에 밀려 사라지는 쪽이 훨씬 나쁘다.

`.unsynced`가 파일명에 드러나므로 운영자도 목록에서 "이 파일은 시각을 믿을 수 없다"를
바로 알아본다. **RTC를 장착하면 이 접미사가 저절로 사라진다** — 규칙을 고칠 필요가 없다.

### 5.2 고정 길이 블록

**샘플 25개를 모아 1232바이트 블록 하나로 적는다.** 블록 종류는 하나뿐이다.

- 단위는 **시간이 아니라 샘플 개수**다. "1초 동안 들어온 것"이 아니라 "25개가 찼을 때".
  덕분에 빈 슬롯이라는 개념이 없다.
- 크기가 고정이라 전원 차단 복구가 **"뒤에서부터 CRC 맞는 블록까지 자르기"** 로 끝난다.
- 25 샘플은 25 Hz에서 약 1초. 온도 갱신 주기와 `fsync` 주기가 블록 하나에 맞아떨어진다.

#### 헤더 (64 바이트, 파일 선두 1회)

| 오프셋 | 항목 | 크기 | 비고 |
| --- | --- | --- | --- |
| 0 | magic `"AHRSBIN\0"` | 8 | |
| 8 | 포맷 버전 | 2 | 현재 1 |
| 10 | 블록 크기 | 2 | 현재 1232 |
| 12 | 블록당 샘플 수 | 2 | 현재 25 |
| 14 | 샘플레이트 (Hz) | 2 | uint16, 25 |
| 16 | 기록 시작 시각 | 8 | double, **장비가 그때 알고 있던 시각** |
| 24 | SENSOR_ID | 16 | 파이의 hostname. 예: `"pi-tilt001"` |
| 40 | SENSOR_FLAG | 2 | 0 미설정 / 1 BASE / 2 MIDDLE / 3 TOP |
| 42 | 장치 번호 | 12 | `0x7F`~`0x84`의 ASCII 시리얼. 예: `"WT4200068151"` |
| 54 | 예약 | 6 | 0으로 채움 |
| 60 | 헤더 CRC32 | 4 | 앞 60바이트에 대해 |

> **절대 시각의 정본은 파일명이다.** 오프셋 16은 장비가 기록을 시작할 때 알고 있던
> 값이고, rename 때 갱신하지 않는다(헤더 덮어쓰기가 §3에서 피하려던 일이다).
> 파일명과 다르면 파일명이 이기며, 그 차이가 곧 보정량이다.

> **장치 번호의 바이트 순서 주의.** 각 레지스터의 **하위 바이트가 먼저**다. Modbus가
> 통신선에서 big-endian이라 상위 바이트 먼저가 당연해 보이고, 그렇게 읽어도 그럴듯한
> 문자열이 나와 틀린 줄 모르기 쉽다. `0x5457 0x3234`는 `WT42`이지 `TW24`가 아니다.
> 반드시 장비에 인쇄된 라벨과 대조한다.

#### 블록 (1232 바이트)

**센서에서 읽는 값을 전부 저장한다.** `readReg(0x34, 15)` 한 번에 9축과 자세각이 모두
담겨 오므로, 일부만 남기는 것은 통신을 아낀 것도 아니고 그저 버리는 것이다.

| 오프셋 | 항목 | 크기 | 형식 |
| --- | --- | --- | --- |
| 0 | 샘플 수 | 2 | uint16, 언제나 25. 다르면 손상으로 본다 |
| 2 | 상태 플래그 | 2 | 아래 |
| 4 | 블록 지속 시간 | 2 | uint16, ms. 첫 샘플 → 마지막 샘플 |
| 6 | 예약 | 2 | |
| 8 | 경과 시간 (ms) | 8 | uint64, 블록 **첫 샘플**의 기록 시작 기준 경과 |
| 16 | 온도 | 4 | float32, ℃ |
| 20 | 예약 | 4 | |
| 24 | **샘플 25개 × 48** | 1200 | 아래 |
| 1224 | 예약 | 4 | |
| 1228 | CRC32 | 4 | 앞 1228바이트에 대해 |

**샘플 (48 바이트)**

| 오프셋 | 항목 | 크기 | 단위 |
| --- | --- | --- | --- |
| 0 | 가속도 X / Y / Z | 12 | float32 × 3, g |
| 12 | 각속도 X / Y / Z | 12 | float32 × 3, °/s |
| 24 | 지자기 X / Y / Z | 12 | float32 × 3, LSB |
| 36 | Roll / Pitch / Yaw | 12 | float32 × 3, ° |

**상태 플래그 비트**

| 비트 | 의미 |
| --- | --- |
| 0 | 이 블록을 채우는 동안 시리얼 읽기 실패가 있었다 |
| 1 | 이 블록에서 시리얼 재연결이 일어났다 |
| 2 | 이 블록 구간이 안정화 기준을 벗어나 있었다 |

#### 읽는 쪽 규칙

- **블록은 언제나 정확히 25개다.** 차지 않은 블록은 **쓰지 않고 버린다** — 정상 종료 때
  모으던 샘플이 남아 있으면 사라진다(1초 미만). 읽는 쪽은 25개가 아닌 블록을 만나면
  **손상으로 보고 거기서 멈춘다.** 잘린 꼬리와 같은 취급이다.
- **블록 지속 시간**은 첫 샘플과 마지막 샘플의 차다. 순조로우면 24틱 = 960 ms.
  이 값이 크면 그 구간에 지연이 있었다는 뜻이고, 상태 플래그 비트 0과 같이 보면 원인을 안다.
- 슬롯 `i`의 시각은 `블록 경과 + i × (1000 / 샘플레이트)` ms. 더 정확히 보려면
  블록 지속 시간을 고르게 나눈다.
- 절대 시각은 **파일명의 시작 시각** + 블록 경과 ms + 슬롯 오프셋으로 복원한다.
- **블록의 경과 시간은 어느 경우에나 유효하다** — 단조 시계 기준이기 때문이다.
  시계를 못 믿어도 "기록 시작 후 3분 12초"는 정확하다. 못 믿는 것은 몇 월 며칠인지뿐이다.

#### 이 포맷이 이렇게 생긴 이유

| 결정 | 근거 |
| --- | --- |
| 샘플이 아니라 블록 단위 | 샘플마다 적으면 데이터 48 B에 부대비용 16 B가 붙는데, 그 16 B가 대부분 같은 값의 반복이다(경과 시간은 고정 틱, 온도는 1초에 한 번 갱신). 묶으면 부대비용이 샘플당 **1.3 B**로 떨어진다 |
| CRC를 블록마다 | `fsync`가 블록마다 일어나므로 **손실 단위가 이미 블록**이다. 샘플 단위 CRC의 정밀도는 쓰일 데가 없다 |
| 원시 레지스터값이 아니라 환산된 값 | 원시는 30 B로 줄지만 **환산 계수가 파일 밖에 남는다.** 자이로·가속도 범위가 설정 레지스터라 누가 바꾸면 그 이후 파일은 다른 계수로 읽어야 한다. 환산해 담으면 **파일 안의 숫자가 곧 물리량**이다. 대가는 하루 65 MB 대신 106 MB |
| TILT를 저장하지 않음 | Roll/Pitch에서 계산되는 값이라 **중복이고, 중복은 둘이 어긋날 여지를 만든다.** 각도를 남기면 "어느 쪽으로 기울었나"에도 답할 수 있다 |
| SENSOR_ID = hostname | 설정 항목이 하나 줄고 빈 값으로 기록될 일이 없다. 장비 하나에 이름 하나가 1:1로 붙는다 |
| 그래도 장치 번호를 함께 | hostname은 **파이의 이름이지 센서의 이름이 아니다.** 파이를 두고 센서만 바꾸면 hostname은 그대로다. 둘이 어긋나면 파일만 보고 드러난다 |
| Modbus 주소·보레이트·모델 문자열 제외 | 통신 설정은 기록된 값의 의미와 무관하고, 모델은 하나뿐이라 매 파일에 같은 상수를 박는 셈이다. 비운 22 B를 장비 식별에 쓴다 |
| 설치 방향·알고리즘도 제외 | 항상 같은 방향으로 설치하기로 정해졌다. 대신 **연결할 때 확인한다**(§11) — 기록에 남겨 나중에 발견하는 것보다, 고칠 시간이 있을 때 알리는 편이 낫다 |
| 시각 품질·부팅 카운터 제외 | 파일명이 이미 담고 있다(§5.1). 헤더에 또 두면 rename 이후 어긋난다 |

**저장하지 않는 센서 값**

- **쿼터니언**(`0x51`~`0x54`) — Roll/Pitch/Yaw와 같은 자세의 다른 표현이라 중복이다.
  짐벌락이 없다는 장점은 ±수도를 다루는 이 용도에서 의미가 없다.
- **모듈 시각**(`0x30`~`0x33`) — RTC가 아니라 R/W 레지스터다. 센서도 전원이 끊기면
  잃으므로 §14의 시각 문제를 풀어주지 못한다.

#### 용량

**샘플당 49.3바이트** (데이터 48 + 부대비용 1.3, 비율 2.6 %).
25 Hz로 **1232 B/초 = 4.3 MB/시간 = 약 106 MB/일**, 32 GB 카드로 **약 300일**.
한 번의 측정이 보통 몇 시간이므로 내려받는 크기는 수십 MB 수준이다.

### 5.3 파일 분할

기본은 **전원이 꺼질 때까지 단일 파일**이다(`segment_minutes = 0`).

현장에서 파일 손상이나 용량 문제가 생기면 `segment_minutes`를 1 등으로 바꿔
분할 기록으로 전환할 수 있게 만든다. 분할 시에는 경계에서 이전 파일을 확정하고
즉시 다음 파일을 연다.

### 5.4 다운로드 시 병합

- 다운로드는 **바이너리 원본 그대로** 내려준다. CSV 변환은 하지 않는다.
- 시간이 연속된 파일들은 **하나로 합쳐서** 내려받을 수 있어야 한다.
  뒤 파일의 헤더를 벗기고 각 블록의 경과 시간을 앞 파일 기준으로 다시 매겨,
  결과물이 그대로 유효한 단일 `.ahrsbin`이 되게 한다. 블록 크기가 고정이라
  재매김은 경과 시간 필드 하나와 CRC만 다시 쓰면 된다.
- 연속 판정 기준: 다음 파일의 시작 시각이 이전 파일의 마지막 샘플 시각 +
  `1/샘플레이트`에서 `merge_gap_tolerance_seconds`(기본 2초) 이내.
- 연속이 끊기면 별개의 그룹으로 나눈다. `/api/files`는 이 그룹 구조를 그대로 보여준다.

---

## 6. 전원 차단과 복구

**전원 차단이 정상 경로다.** 현장에서 기록을 멈추는 방법은 전원을 내리는 것뿐이므로,
"종료할 때 처리한다"에 기대는 설계를 두면 안 된다. 파일을 확정하고 이름을 바로잡는
일은 전부 **다음 부팅의 복구 단계**에서 일어난다. 웹에서 정지시키는 정상 종료도
같은 함수를 호출할 뿐, 드물게 쓰이는 경로다.

### 기록 중

- 쓰기는 항상 **블록 단위**다. 1232바이트를 통째로 쓰고 바로 `fsync`한다.
  블록을 채우는 동안의 샘플 25개는 메모리에 있으므로, 전원이 끊기면 **최대
  한 블록(약 1초)** 을 잃는다.
- `.timeinfo`는 원자적으로 쓴다(임시 파일 → `fsync` → rename → 디렉터리 `fsync`).
  NTP가 붙는 순간 한 번뿐이라 비용이 없다시피 하다.
- 확정된 파일에는 절대 다시 쓰지 않는다. 그래야 이전 기록이 새 전원 차단에
  말려들지 않는다.

**파일을 주기적으로 닫았다 여는 방식은 쓰지 않는다.** 근거를 남겨 둔다.

- `close()`는 디스크에 내려보내지 않는다. `close(2)` 매뉴얼에 명시돼 있듯
  *"A successful close does not guarantee that the data has been successfully
  saved to disk"* 이며, 파일 기술자를 반납할 뿐 데이터는 페이지 캐시에 남을 수 있다.
  디스크로 내리는 것은 `fsync()`다. 따라서 `write + fsync`가 `write + close`보다
  **강하다.** 닫기만 해서는 보호가 늘지 않고 오히려 준다.
- `write + fsync + close`로 해도 `close`는 durability에 아무것도 더하지 못하면서
  inode 갱신과 재열기 비용만 늘린다.
- 파일시스템 구조 자체는 ext4의 저널링(`data=ordered`)이 지킨다. 전원이 끊겨도
  파일시스템이 깨지지는 않고, 잃는 것은 아직 동기화되지 않은 마지막 블록뿐이다.
- 라즈베리파이에서 카드가 실제로 깨지는 원인은 파일을 여닫는 방식이 아니라
  **SD 카드가 erase block을 프로그래밍하는 도중 전원이 끊기는 것**이다. FTL 내부에서
  일어나므로 어떤 파일 API로도 막을 수 없다. 아래 UPS 권장이 유일한 실질적 해법이다.

**`fsync` 주기는 조절 가능하다.** 블록 하나가 1232바이트라 SD의 4 KB 페이지 하나에
들어가므로 쓰기 증폭은 약 3배다. 하루 약 345 MB가 물리적으로 기록되는데, 32 GB
카드의 수명(수십 TB) 대비로는 문제없는 수준이다. 손실 폭을 늘려도 된다면
`record_fsync_interval_seconds`를 올려 블록 여러 개를 모아 동기화할 수 있다.
기본 1초는 **"최대 1초까지만 잃는다"** 를 택한 값이다.

### 부팅 시 복구

남아 있는 `.partial`마다 아래를 수행한다. HTTP 대기(§3 절차 3)보다 **먼저** 끝낸다.

1. 헤더 CRC를 확인한다. 깨졌으면 `data/corrupt/`로 옮기고 로그를 남긴다.
2. 뒤에서부터 CRC가 통과하는 마지막 블록을 찾아 그 지점까지 truncate한다.
   블록 크기가 고정이므로 경계를 다시 찾을 필요가 없다.
3. 같은 이름의 `.timeinfo`를 읽는다.
   - 보정값이 있으면 그 시각으로 최종 파일명을 짓고 `.unsynced` 접미사를 뗀다.
   - 없거나 깨졌으면 헤더의 장비 기준 시각을 그대로 쓰고 `.unsynced`를 유지한다.
     **데이터는 온전하고 시각만 못 믿는 상태**이며, 그 사실이 이름에 드러난다.
4. `.recovered.ahrsbin`으로 rename하고, `.timeinfo`도 같은 이름으로 맞춘다.

복구 전 `.partial`은 `/api/files`의 일반 목록에 넣지 않는다.

> **권장 하드웨어**: 슈퍼커패시터 UPS HAT 또는 전원 유지 회로를 붙이면 차단
> 시점에 정상 종료 절차를 밟을 수 있다. 없이도 위 복구 절차로 마지막 1초
> 이내 데이터만 잃는다.

---

## 7. HTTP API

포트 8080. 응답은 JSON(다운로드 제외).

| API | 설명 |
| --- | --- |
| `GET /api/status` | **SENSOR_ID·SENSOR_FLAG**, 상태, 현재 파일, 경과 시간·샘플 수, 마지막 안정화 판정 상세, 센서 연결 상태, 설정 버전 |
| `GET /api/files` | 확정 파일 목록. 파일명, 크기, 시작·종료 시각, 샘플 수, 복구 여부, **연속 그룹 ID** |
| `GET /api/files/{filename}` | 단일 파일 다운로드 |
| `GET /api/groups/{group_id}` | 연속 그룹 전체를 병합한 단일 `.ahrsbin` 다운로드 |
| `DELETE /api/files/{filename}` | 파일 삭제 (PIN 필요) |
| `POST /api/record/start` | `maintenance` 상태에서 수동 기록 시작 |
| `POST /api/record/stop` | 기록 중지 및 파일 확정 |
| `POST /api/files/{filename}/collected` | 받았음을 확인. 이 요청이 휴지통 이동을 일으킨다 |
| `POST /api/groups/{group_id}/collected` | 그룹 전체를 받았음을 확인 |
| `GET /settings` | 설정 조회·수정 화면 |
| `POST /settings` | 설정 저장 |

**파일명 검증**: 경로 구분자와 `..`를 허용하지 않고,
`FLAG_[bNNNN_]YYYYMMDD_HHMMSS[.unsynced][.recovered].ahrsbin` 정규식에 정확히 맞는 것만
받는다. 검증 후에도 실제 열기 전에 `data/` 하위인지 `realpath`로 다시 확인한다.

**시각·파일명 규칙**: `/api/files`와 `/api/status`가 돌려주는 시각은 시각 보정
블록을 적용한 **보정 후 값**이다. 보정 여부와 원래 값도 함께 실어, 운영자가
"이 시각이 나중에 고쳐진 것"임을 알 수 있게 한다.

다운로드 응답의 `Content-Disposition`에는 **보정된 시각의 파일명**을 싣는다(§3).
디스크상의 이름이 아직 옛 이름이어도 운영자가 받는 파일은 정확한 이름을 갖는다.
그룹 병합 다운로드(`/api/groups/{id}`)도 같은 규칙을 따르며, 첫 파일의 보정된
시작 시각으로 이름을 짓는다.

### 다운로드한 파일은 장비에서 지운다

내려받은 파일은 장비에 남기지 않는다. 목록이 깨끗해지고, 다음 측정 때 예전 파일과
헷갈릴 일이 없으며, SD 카드도 비워진다.

**다만 곧바로 파기하지는 않는다.** §1의 현장 조건이 이유다. 차량에서 멀어지면
WiFi가 끊기므로 **다운로드가 중간에 끊기는 일이 실제로 자주 일어난다.** 전송이
끊긴 걸 삭제로 이어 버리면 그 측정은 영영 사라진다. 그래서 두 단계로 나눈다.

1. **받았다는 확인이 온 경우에만** 삭제 대상으로 삼는다.

   > 서버가 "다 보냈다"로 판단하면 안 된다. 응답 생성이 끝났다는 것은 바이트가
   > 소켓에 들어갔다는 뜻이지 운영자에게 닿았다는 뜻이 아니다. 기록 파일은 흔히
   > 전송 청크보다 작아서 **한 번에 다 나가고 루프가 끝나 버린다.** 실측에서
   > 13,632바이트 파일을 1,600바이트만 받고 끊었는데도 서버는 완료로 판단했다.
   > HTTP에는 전달 확인 신호가 없으므로, 서버 혼자서는 알 수 없다.

   그래서 클라이언트가 알려 준다. 운영자 화면은 본문을 **전부 읽어 저장까지 마친
   뒤** `POST /api/files/{filename}/collected`를 보내고, 이 요청이 삭제를 일으킨다.
   중간에 끊긴 연결은 여기까지 오지 못하므로 파일이 목록에 그대로 남는다.

   단순 `GET` 다운로드는 아무것도 치우지 않는다. `curl`이나 스크립트로 사본을
   받아 가는 동안 장비에서 파일이 조용히 사라지면 안 된다.
2. 삭제는 파기가 아니라 **`data/trash/`로 이동**이다. `/api/files` 목록에서는
   즉시 사라지므로 운영자 입장에서는 "지워진" 것과 같다.
3. 휴지통의 파일은 `trash_retention_days`(기본 7일)가 지나면, 또는 디스크 여유가
   `min_free_mb` 아래로 내려가면 **오래된 것부터** 실제로 삭제한다.
4. 그동안은 개발자가 SSH로 되살릴 수 있다. 운영자에게는 이 경로를 노출하지 않는다.

세부 규칙:

- 그룹 병합 다운로드는 `POST /api/groups/{id}/collected`로 확인하며, **그 그룹에
  속한 파일 전부**를 휴지통으로 옮긴다. 운영자가 받은 것이 그 전체이기 때문이다.
- **기록 중인 파일은 어떤 경우에도 지우지 않는다.** 확정된 파일만 대상이다.
- `delete_after_download`를 `false`로 두면 이 동작 전체를 끈다. 현장 검증 기간이나
  중요한 측정에서는 꺼 두는 편이 안전하다.
- 휴지통으로 옮긴 시각과 원래 파일명을 로그에 남긴다. "분명히 받았는데 없다"는
  문의가 왔을 때 추적할 유일한 근거다.

---

## 8. 웹 설정 화면

내부망 전용의 단순 HTML. 프레임워크 없이 서버 렌더링으로 충분하다.

**모든 화면 상단에 `SENSOR_ID · SENSOR_FLAG`를 크게 표시한다.** 한 현장에 세 대가
같은 WiFi에 붙어 있으므로, 지금 보고 있는 것이 어느 장비인지 알 수 없으면 엉뚱한
장비의 설정을 바꾸거나 엉뚱한 파일을 받게 된다.

> **`.local` 이름에 기대지 않는다.** 통신사 DNS가 없는 도메인에 자기네 주소를
> 돌려주는 환경에서는 mDNS 응답이 묻혀 `pi-tilt001.local`이 엉뚱한 곳으로 간다
> (실측에서 `218.38.137.28`로 해석됐다). 장비 쪽 avahi는 정상이므로 고칠 것이 없고,
> **접속은 IP로 한다.** 그래서 e-paper 하단에 IP가 나온다(§9). 이름으로 접속하고
> 싶으면 공유기에서 고정 IP를 배정하고 클라이언트 `hosts`에 적는다.

**화면에 두는 것은 운영자가 정말로 정해야 하는 것뿐이다.**

| 항목 | 이유 |
| --- | --- |
| 설치 위치 (BASE / MIDDLE / TOP) | 지정하지 않으면 파일명이 `UNSET_`이 되어 나중에 어느 높이인지 알 수 없다 |
| 접속 대기 시간 | 현장에서 접속이 늦어지면 늘려야 한다 |
| 파일 분할 주기 | 0이면 전원이 꺼질 때까지 한 파일. 현장에서 바꿀 수 있게 둔다 |
| 다운로드한 파일 정리 여부 | 운영자가 보는 목록이 달라진다 |
| 휴지통 보관 기간 | 받은 파일을 얼마나 남길지 |
| 최소 여유 공간 | 카드가 찰 때의 동작 |

**나머지는 화면에 두지 않는다.** 안정화 기준, 샘플레이트, `fsync` 주기 같은
값은 공학적으로 정해진 것이지 현장에서 고를 것이 아니다.
특히 **안정화 기준을 손으로 만지면 기록이 아예 시작되지 않거나 흔들리는 중에
시작된다.** 화면에 있는 설정은 언젠가 누군가 바꾼다. 필요하면 SSH로 접속해
`config.json`에서 다룬다.

설치 위치는 선택 항목이며 자유 입력이 아니다 — 오타가 나면 파일명이 어긋난다.
바꾸면 다음 기록부터 적용되고 진행 중인 기록의 헤더는 그대로다. 값이 바뀌면
e-paper를 즉시 갱신해 현장에서 바로 확인할 수 있게 한다.

SENSOR_ID는 hostname이므로 이 화면에 없다(§5.2).

- `config.json`을 직접 덮어쓰지 않는다. 임시 파일 → `fsync` → 원자적 rename 순서.
- 저장 후 현재 적용값을 다시 표시한다.
- **기록 중에는 진행 중인 기록의 기준값을 유지하고,** 새 값은 다음 안정화 판정부터 적용한다.
- 시리얼 포트명, 데이터 경로, PIN 해시는 화면에 노출하지 않는다.
- **시각 상태를 항상 보여준다.** 현재 장비 시각, 품질(`rtc`/`synced`/`unsynced`/
  `invalid`), 마지막 NTP 동기화 시각. 품질이 `unsynced`나 `invalid`면 눈에 띄게
  경고한다. 설정할 것은 없다 — 이 화면이 보인다는 건 네트워크가 있다는 뜻이므로
  잠시 뒤 NTP가 알아서 맞춘다(§1). 운영자에게는 "곧 자동으로 맞춰집니다"로 안내한다.
- 삭제와 설정 저장은 기존 `app_config.verify_pin()`으로 보호한다. 조회와 다운로드는
  내부망 전제로 무인증. 외부 노출이 필요해지면 VPN이나 리버스 프록시를 앞에 둔다.

---

## 9. e-paper 상태 표시

브라우저 없이 현장에서 상태를 확인할 유일한 수단이므로 요구사항에 포함한다.

패널은 **측정 상태 표시이자 장비에 붙은 명패**다. 전원이 없어도 이미지가 남으므로,
꺼진 채 현장에 놓여 있어도 이 장비가 무엇이고 어디에 어떻게 설치되는지 계속 말해 준다.
e-paper를 고른 값어치가 여기서 나온다.

#### 화면은 셋이다

200×200에 세 가지를 다 담을 수 없고, **무엇이 중요한지가 순간마다 완전히 달라진다.**
장비를 다는 중, 놓아두고 재는 중, 파일을 받으러 온 중 — 셋은 서로 다른 질문에 답한다.

| 화면 | 언제 | 내용 |
| --- | --- | --- |
| **설치** | 전원 투입부터, 기록 시작 후 1분까지 | 축 그림 + 접촉면 (1.45배) |
| **측정** | 기록이 1분 넘게 진행되면 | 기울기 · 온도 · 상태/경과/샘플 수 |
| **GEOLUX** | 접속 중(`maintenance`) | 이름 |

기록 시작 후 1분까지 설치 화면을 유지하는 이유는 **그때까지 운영자가 아직 크레인
위에 있을 수 있어서**다. 다 달고 내려온 뒤에야 측정값이 의미가 있다.

접속 중에 이름만 띄우는 이유는, 그 순간 **패널이 해줄 말을 브라우저가 더 잘 하기
때문**이다. 화면에 남길 만한 것이 장비의 정체성밖에 없다.

```text
  0 ┌────────────────────────────────┐
    │▓ pi-tilt001             TOP   ▓│  26  반전: SENSOR_ID · 설치 위치 (공통)
 26 ├────────────────────────────────┤
    │                                │
    │          화면별 본문            │ 130
    │                                │
156 ├────────────────────────────────┤
    │ 192.168.0.19                   │  44  접속 주소
    │▓wifi Geolux_RND               ▓│      SSID (붙어 있으면 반전) (공통)
200 └────────────────────────────────┘
```

#### 시각을 표시하지 않고 SSID를 표시한다

시계는 틀릴 수 있다(§3). **틀린 시각은 없느니만 못하고**, 믿을 수 있는 시각은 이미
파일명에 있다. 그 자리를 SSID가 대신한다.

- **접속된 SSID가 아니라 접속해야 할 SSID다.** 크레인 위에서는 아무 데도 안 붙어
  있는데, 바로 그때 어느 망을 찾아야 하는지 알아야 한다. 그래서 저장된 프로파일에서
  읽는다(NetworkManager).
- **붙어 있으면 반전으로 표시한다.** 페이지가 안 열릴 때 운영자가 가장 먼저 알아야
  하는 것이 "붙었나 안 붙었나"이고, 검은 띠 하나면 글자를 읽지 않고도 멀리서 답이 된다.
- IP는 그 위에 그대로 둔다. **`.local` 이름은 믿을 수 없으므로**(§8) 현장에서
  접속할 수 있는 유일한 주소다.

#### 판정하지 않는다

**기울기가 허용 범위인지는 이 장비가 정하지 않는다.** 파일을 가져가는 서버가 정한다.
패널은 값을 보여줄 뿐 `ALARM`을 띄우지 않고, 부저도 릴레이 출력도 없다.

> 판정을 장비에 두면 기준이 두 곳(장비와 서버)에 생긴다. 둘이 어긋나는 순간 어느
> 쪽이 맞는지 알 수 없게 된다. **기록해서 넘기는 것까지가 이 장비의 일이다.**

같은 이유로 Roll/Pitch도 패널에 두지 않는다. 방향까지 읽어야 하는 작업은 파일을
받아 간 쪽에서 하고, 현장에서는 "기록되고 있는가"만 확인하면 된다.

#### 축 그림 하나로 축방향과 접촉면을 같이 답한다

둘은 따로 그릴 것이 아니다. 기구를 상자로 그리고 X/Y/Z를 얹은 뒤, 구조물에 닿는
면에 빗금을 치면 **"어느 쪽이 위인가"와 "어느 면을 붙이는가"에 한 그림이 동시에
답한다.** 200×200에서 두 그림을 따로 넣을 자리도 없다.

- **Z는 위를 가리킨다.** 실측으로 정지 상태의 `AccZ`가 **+1.000 g**이고, 가속도계는
  위를 향한 축에 +1 g를 싣는다. 그림을 반대로 그리면 **명패가 파일 안의 부호와
  어긋나게** 되므로 그렇게 두지 않는다.
- **접촉면은 기구의 아랫면**이다. 상자의 아래 변에 빗금을 치고 바로 아래
  `접촉면`이라고 적어 화살표 없이도 읽히게 한다. Z가 접촉면의 반대 방향이라는 것도
  그림에서 바로 보인다.
- 기구가 바뀔 수 있으므로 `contact_face`로 두되 기본값은 `bottom`이다. 운영자가
  고를 것이 아니므로 설정 화면에는 없다(§8).

#### 한글 폰트

파이에 기본 설치된 것은 DejaVu뿐이라 한글이 네모로 나온다.
`sudo apt install fonts-nanum`을 설치 절차에 넣는다.

폰트가 없으면 `접촉면`을 `CONTACT`로 대체하고 로그에 한 번 남긴다 — **다른 언어로라도
읽히는 편이 네모가 늘어선 것보다 낫다.**

#### 화면 회전

패널은 기구에 **시계방향으로 90도 돌아간 채** 붙는다. 그리기는 똑바로 선 좌표계에서
하고 **완성된 프레임만 마지막에 돌린다**(`panel_rotation`, 기본 90).

축 그림도 함께 돌아간다. 기구째로 돌아가 있으므로 장비를 실제 설치 자세로 놓고 보면
Z가 위를 가리킨다 — 그림과 실제 축이 맞는다.

글자를 처음부터 옆으로 눕혀 그리면 이 절의 모든 좌표를 머릿속에서 돌려 가며 확인해야
한다. 패널이 정사각형이라 회전해도 크기가 그대로이므로 다시 맞출 것도 없다.

미리보기(`--out`)에도 같은 회전이 적용된다. 화면에서 본 것과 장비에서 볼 것이
달라지면 미리보기의 의미가 없다.

#### 갱신 정책

- 주기는 `panel_refresh_seconds`(**기본 60초**). 패널은 전원 없이 이미지를 유지하므로
  더 자주 갱신할 이유가 없다.
- 상태 전환(기록 시작·종료, 오류)에는 주기와 무관하게 **즉시 갱신**한다.
- **읽는 사람이 알아챌 변화가 없으면 갱신을 건너뛴다.** 패널은 갱신 횟수로 수명이
  정해지는데, 가만히 있는 장비가 같은 그림을 다시 그리며 수명을 쓸 이유가 없다.
  화면 종류가 바뀌는 것도 변화로 센다.
- **주기를 초 단위로 낮추지 말 것.** 근거를 남겨 둔다.
  - 전체 갱신 1회에 약 1.4초가 걸린다. 1초 주기는 갱신 시간보다 짧아 성립하지 않는다.
  - 현재 드라이버([gdey0154d67.py:170](gdey0154d67.py#L170))는 매번 RAM과 RAM_PREV를
    모두 써서 **항상 전체 갱신**한다. 부분 갱신 경로가 없고 `UPDATE_FAST`는 값이
    `UPDATE_FULL`과 같은 자리표시자다.
  - e-paper 수명은 갱신 횟수로 정해진다(통상 100만 회 수준). 60초 주기면 하루
    1,440회로 수년을 쓰지만, 1초 주기면 하루 86,400회라 **약 2주면 소진**된다.
  - 실시간 값이 필요하면 브라우저의 `/api/status`를 쓴다.

#### 그 밖의 규칙

- **SENSOR_FLAG가 미설정이면** 위치 자리에 `UNSET`을 반전 표시한다. 파일명의
  `UNSET_`과 같은 신호를 현장에서도 보이게 하는 것이다.
- **네트워크 상태를 보여준다.** 차량에서 멀어지면 WiFi가 끊기므로(§1) IP 자리에
  `NO NETWORK`를 표시하고, SSID의 반전이 풀린다. "지금 연결돼 있나"를 패널만 보고
  알 수 있어야 한다. 시각 품질은 패널에 표시하지 않는다 — 시각 자체를 안 보여주기
  때문이고, 믿을 수 있는지는 파일명의 `.unsynced`로 드러난다.
- **패널 갱신이 센서 폴링 루프를 막으면 안 된다.** 별도 스레드에서 처리하고,
  기록 스레드는 최근 값 스냅샷만 넘긴다. 1.4초를 폴링 루프에서 쓰면 매 분 35샘플을 잃는다.
- **패널이 없거나 초기화에 실패해도 기록은 계속된다.** 화면이 없다고 측정을 잃는
  것이 이 프로젝트에서 가장 나쁜 결과다.

---

## 10. 설정 항목

전부 `config.json`의 `recorder` 섹션에 있다. 나뉘는 것은 **누가 바꿀 수 있는가**다.

#### 웹 설정 화면에 있는 것 (§8)

운영자가 현장에서 실제로 정해야 하는 값들이다.

| 설정 | 기본값 | 설명 |
| --- | --- | --- |
| `sensor_flag` | `unset` | `base` / `middle` / `top`. 선택 항목이며 자유 입력이 아니다 |
| `mount_delay_seconds` | 60 | **항상** 기다린다. 크레인에 올라가 장비를 다는 시간 |
| `http_wait_seconds` | 60 | 주소가 나온 뒤 접속을 기다리는 시간. 네트워크가 있을 때만 |
| `network_wait_seconds` | 90 | 붙은 뒤 DHCP 주소를 기다리는 시간 |
| `segment_minutes` | 0 | 파일 분할 주기 (0 = 전원이 꺼질 때까지 한 파일) |
| `delete_after_download` | true | 받은 파일을 휴지통으로 옮김 |
| `trash_retention_days` | 7 | 휴지통 보관 기간 |
| `min_free_mb` | 500 | 이 아래로 남으면 휴지통부터 비우고, 그래도 모자라면 기록 중지 |

#### 설정 파일에서만 바꾸는 것

**공학적으로 정해진 값이라 현장에서 고를 것이 아니다.** 특히 안정화 기준을 손으로
만지면 기록이 아예 시작되지 않거나 흔들리는 중에 시작된다. 화면에 있는 설정은
언젠가 누군가 바꾼다.

| 설정 | 기본값 | 설명 |
| --- | --- | --- |
| `stability_window_seconds` | 5 | 안정화 판정 시간 창 |
| `stability_min_samples` | 100 | 판정 최소 샘플 수. 25 Hz에서 100개 = 4초 |
| `gyro_rms_max_dps` | 0.5 | 자이로 RMS 기준 |
| `accel_std_max_g` | 0.01 | 가속도 표준편차 기준 |
| `attitude_std_max_deg` | 0.3 | 자세각 표준편차 기준 |
| `stop_on_unstable` | false | 불안정 지속 시 기록 중단 여부 |
| `record_fsync_interval_seconds` | 1 | 강제 동기화 주기. 블록마다 동기화하므로 사실상 블록 주기 |
| `merge_gap_tolerance_seconds` | 2 | 연속 파일 판정 허용 간격 |
| `ntp_retry_interval_seconds` | 30 | 백그라운드 NTP 동기화 확인 주기 |
| `time_save_interval_seconds` | 10 | 마지막으로 알던 시각 저장 주기 |
| `min_valid_year` | 2026 | 이보다 이전이면 시각을 `invalid`로 판정 |
| `contact_face` | `bottom` | 구조물에 닿는 면. 기구가 정하는 값이다 |
| `panel_refresh_seconds` | 60 | e-paper 갱신 주기. **초 단위로 낮추지 말 것**(§9) |
| `panel_rotation` | 90 | 패널이 기구에 돌아간 채 붙으므로 완성된 화면을 시계방향으로 돌린다 |
| `http_port` | 8080 | 웹 포트 |

#### 여기에 없는 것

- **SENSOR_ID** — 파이의 hostname이다(§5.2). 설정 항목이 아니다.
- **시리얼 포트** — `port_config.py`가 정한다. `--port`나 `VERTICRANE_PORT`로 덮는다.
- `LOG_LEVEL`은 기존 최상위 키를 그대로 쓴다.
- `slope_threshold_pct`와 `ma_seconds`는 개발자 도구(`dev/`의 대시보드와 분석)에만
  남아 있고 **기록기는 읽지 않는다** — 판정을 하지 않기 때문이다(§9).

---

## 11. 오류 처리와 로깅

- 시리얼 연결 해제, 읽기 실패, 파싱 오류로 **프로세스가 죽으면 안 된다.**
  지수 백오프(1초 → 최대 30초)로 재연결을 시도하고, 그 사이에도 웹은 계속 응답한다.
- 재연결 중에도 파일은 열어 둔 채로 두고, 복구되면 같은 파일에 이어 쓴다.
  끊긴 구간은 블록 지속 시간이 길어지는 것으로 드러나며, 재개된 블록의 상태
  플래그 비트 1에 표시한다.
- 오류 상태는 `/api/status`와 e-paper에 노출한다.
### 센서 설정 확인

연결할 때마다 아래를 읽어 기대값과 맞는지 확인한다. 다르면 **경고를 로그와
`/api/status`에 남기고 e-paper에도 표시하되, 기록은 계속한다.** 설정이 어긋난
데이터라도 없는 것보다는 낫고, 판단은 사람이 한다.

| 레지스터 | 항목 | 기대값 | 어긋나면 |
| --- | --- | --- | --- |
| `0x23` | 설치 방향 (ORIENT) | 0 (수평) | Roll/Pitch가 가리키는 축이 달라진다 |
| `0x24` | 알고리즘 (AXIS6) | 1 (6축) | 지자기 간섭이 자세각에 섞여 든다 |
| `0x1F` | 대역폭 | 5 = 10 Hz | 공장 기본값은 4 = 20 Hz다. 25 Hz 샘플링의 Nyquist 전제가 깨진다 |

세 값 모두 `dev/configure_sensor.py`가 설정하는 것들이다. **경고 문구에는 원시 숫자가
아니라 값의 의미를 적는다** — 숫자만 나오는 경고는 아무도 대응할 수 없고, 기대값을
잘못 적어 두면 사람이 경고 자체를 무시하게 된다. 현장에서 장비를 교체했는데
설정을 안 한 경우가 이 확인에 걸린다.

- 로깅은 기존 loguru 설정(`app_config.setup_logging()`)을 그대로 쓴다. 기록할 이벤트:
  부팅, HTTP 접속 감지, 상태 전환, 안정화 판정 결과, 기록 시작·종료, 파일 복구 결과,
  시리얼 오류·재연결, 설정 변경, 센서 설정 확인 결과, **시각 동기화 결과와 기록 중 시각 보정**.
- 시각이 보정되면 보정 전후 값과 차이를 그대로 남긴다. 나중에 파일이 왜 나뉘었는지
  설명할 수 있는 유일한 근거다.
- 디스크 여유 공간을 주기적으로 확인한다. `min_free_mb` 미만이면 **먼저 휴지통을
  오래된 것부터 비우고**(§7), 그래도 모자라면 경고 후 기록을 중지한다. SD 카드가
  가득 찬 상태로 계속 쓰면 파일시스템이 깨진다.
- 다운로드 완료·휴지통 이동·휴지통 실제 삭제를 각각 로그에 남긴다.

---

## 12. 개발자 전용 도구 (숨김)

지금까지 만든 것들 — Streamlit 대시보드, FFT·통계 분석, 센서 설정, e-ink 테스트 — 은
**운영자에게 노출하지 않는다.** 웹에서 접근할 수 있는 숨겨진 링크나 URL도 만들지 않는다.

접근 방법은 **SSH 접속 후 CLI 실행**뿐이다. 대부분 시리얼 포트를 직접 열기 때문에
먼저 운영 서비스를 멈춰야 한다.

```bash
ssh pi@192.168.0.19
cd ~/verticrane
sudo systemctl stop verticrane-recorder     # 시리얼 포트 반납
.venv/bin/python configure_sensor.py --baud 115200
./run_dashboard.sh                          # 필요할 때만
sudo systemctl start verticrane-recorder    # 끝나면 되돌리기
```

정리할 것:

- 개발자용 파일들을 `dev/`로 옮겨 운영 코드와 눈으로 구분되게 한다.
  (`dashboard.py`, `pages/setup.py`, `analyze_tilt.py`, `log_tilt.py`,
  `configure_sensor.py`, `eink_status.py`, `eink_test.py`, `gpio_blink.py`,
  `test.py`, `run_dashboard.sh`)

  > **`read_status.py`는 옮기지 않는다.** CLI이기도 하지만 `recorder.py`가
  > `connectAutoBaud()`와 `decode_numberid()`를 쓰는 공용 모듈이다. 옮기면
  > 기록기가 뜨지 않는다. `hwt9037_485.py`, `port_config.py`, `app_config.py`,
  > `gdey0154d67.py`도 같은 이유로 루트에 남는다.

  `dev/` 스크립트는 한 단계 아래에 있으므로 루트를 `sys.path`에 넣는 두 줄이
  파일 앞머리에 들어간다. 마법 같은 부트스트랩 import 대신 명시적으로 두어,
  왜 필요한지가 문제가 생기는 자리에 그대로 보이게 한다.
- `verticrane-dashboard.service`는 `systemctl disable`하고, 저장소에는 참고용으로 남긴다.
- 서비스 중지 → 실행 → 재시작을 한 번에 하는 `dev/devmode.sh`를 만든다.
  중간에 죽어도, Ctrl-C를 눌러도, 명령이 실패해도 서비스가 되살아나게 `trap`을 건다.
  **손으로 멈추는 것은 쉽지만 다시 켜는 것을 잊기는 더 쉽고**, 조용히 기록을
  멈춘 현장 장비가 이 프로젝트에서 가장 나쁜 결과다.
- `.ahrsbin` → CSV 변환기 `dev/ahrsbin_to_csv.py`를 만든다. `--report`를 주면
  변환 후 `analyze_tilt.py`까지 돌린다. 기존 `analyze_tilt.py`가
  CSV를 받으므로, 이 변환기 하나로 기존 분석 자산이 새 포맷에 다시 붙는다.
  열은 기존 `log_tilt.py` CSV와 같다: `timestamp, elapsed_s, Roll_deg, Pitch_deg,
  Yaw_deg, slope_pct, AccX_g, AccY_g, AccZ_g, GyroX_dps, GyroY_dps, GyroZ_dps,
  Temp_C`. `slope_pct`는 Roll/Pitch에서 계산해 채우고, 온도는 블록당 하나이므로
  같은 블록의 25행에 같은 값이 반복된다. 지자기 3열이 추가된다. `analyze_tilt.py`의
  Roll/Pitch·자이로 구간은 데이터가 없으면 건너뛰는 가드가 이미 있어
  ([analyze_tilt.py:124](analyze_tilt.py#L124)) 그대로 동작한다.
- README를 운영자용(설치·접속·다운로드)과 개발자용으로 나눈다.

---

## 13. 산출물

**운영 — systemd가 띄우는 것 (저장소 루트)**

| 파일 | 역할 |
| --- | --- |
| `recorder.py` | 기록 루프와 상태 기계 (§3, §4) |
| `ahrs_file.py` | `.ahrsbin` 포맷 — 헤더·블록·복구·병합 (§5, §6) |
| `stability.py` | 안정화 판정 (§4) |
| `filestore.py` | 파일 목록·연속 그룹·휴지통 (§5.4, §7) |
| `web/` | 운영자 웹 — Flask, 8080 (§7, §8) |
| `eink_panel.py` | e-paper 화면 (§9) |
| `read_status.py` | 센서 연결·상태 읽기 — **기록기가 사용한다** |
| `hwt9037_485.py`, `port_config.py`, `app_config.py`, `gdey0154d67.py` | 기존 모듈 |
| `verticrane-recorder.service` | systemd 유닛 |

**개발자 — SSH로만 (`dev/`)** — §12 참조.

**문서**

| 파일 | 내용 |
| --- | --- |
| 이 문서 | 포맷 명세, API, 설계 근거 |
| `README.md` | 운영자용 사용법 / 설치 / 개발자용 |
| `agent.md` | 구조와 이 코드베이스에서 틀리기 쉬운 것들 |
| `doc/protocol.md` | 센서 레지스터 맵 |
| `doc/raspberry_pi.md` | 파이 설치 상세 |

`requirements.txt`에 Flask를 더하고, 설치 절차에 `fonts-nanum`을 넣는다(§9).

### 검증

**하드웨어 없이** — 이 셋은 언제나 통과해야 한다.

```bash
python test_ahrs_file.py     # 쓰기 → 중간에 자르기 → 복구 → .timeinfo → 병합 → 파일명
python test_stability.py     # 0/360 경계, 윈도우 경계, 움직임 거부
python stability.py data/*.csv   # 기록된 로그로 기준값 재검증
```

**장비에서** — 요구사항별로 무엇을 보면 되는지.

| 확인할 것 | 방법 |
| --- | --- |
| §3 접속 없으면 자동 기록 | 인자 없이 띄우고 60초 건드리지 않는다 → `recording`으로 간다 |
| §3 접속하면 대기 | 60초 안에 `/api/status`를 부른다 → `maintenance`에 머문다 |
| §4 안정화 판정 | 기록 중 흔들어 본다 → 로그에 사유가 남고 블록에 플래그가 붙는다 |
| §6 **전원 차단 복구** | 기록 중 전원을 뽑는다 → 다음 부팅에서 `.recovered`로 확정된다 |
| §7 다운로드 중단 | 전송을 끊는다 → 파일이 목록에 그대로 남는다 |
| §7 경로 검증 | `/api/files/../../etc/passwd` → 404 |
| §9 패널 | `eink_panel.py --out panel.png`로 하드웨어 없이도 미리 본다 |

§6은 **직접 전원을 뽑아서** 확인한다. 정상 종료로는 그 경로를 타지 않는다.

---

## 14. RTC 장착 (예정)

**RTC 모듈(DS3231 등)을 붙이기로 한다. 다만 지금은 부품이 없어 그 자리만 비워 둔다.**

§1의 현장 조건이 RTC의 필요성을 그대로 보여준다. **장비를 들고 차량에서 멀어져
전원을 켜는 순간이 바로 기록이 시작되는 순간인데, 그때 네트워크가 없다.** §3의
장치들(마지막 시각 이어가기, 백그라운드 감시, 복구 단계의 이름 보정)은 이 구멍을
메우는 것이지 없애는 게 아니다. 남는 한계는 이렇다.

- 차량이 끝내 가까워지지 않으면 그 기록은 `unsynced`로 끝난다. 데이터는 온전하지만
  절대 시각을 못 믿는다.
- 소급 적용이 되더라도, 기록 시작부터 WiFi 복귀까지의 오차는 "전원 끄고 이동한 시간"
  만큼이다. 이동이 길면 오차도 길다.

RTC를 달면 이 두 한계가 **동시에** 사라진다. 부팅 즉시 정확한 시각이 나오므로
`.timeinfo`의 보정값이 쓰일 일도 없어진다.

이번 구현은 RTC 없이 동작해야 하고, 나중에 모듈을 꽂을 때 **파일 포맷도 파일명
규칙도 바꾸지 않아야 한다.** 그러려면 지금 아래를 미리 확보해 둔다.

| 미리 확보할 것 | 이유 |
| --- | --- |
| 시각 품질을 파일명·`.timeinfo`로 분리 | 포맷을 건드리지 않고 `rtc`를 추가할 수 있음 (§3) |
| 시각 획득 경로를 우선순위 표로 분리 | RTC를 1순위에 끼워 넣기만 하면 됨 (§3) |
| `.unsynced` 접미사 + 부팅 카운터 | RTC가 붙으면 저절로 사라짐 (§5.1) |
| 복구 단계에서 이름을 확정하는 구조 | RTC가 붙으면 첫 이름부터 정확해질 뿐 흐름은 같음 (§6) |
| I2C 활성화 여부를 설치 문서에 기재 | `dtparam=i2c_arm=on` — 배선 전에 준비 |

장착 시 실제로 할 일은 이 정도로 끝나야 한다.

- `/boot/firmware/config.txt`에 `dtoverlay=i2c-rtc,ds3231` 추가
- `fake-hwclock` 제거
- 시각 획득 함수에 RTC 경로 한 갈래 추가, 품질 `rtc` 반환
- 코드 그 외 부분, 특히 `ahrs_file.py`는 손대지 않는다

> 장착 전까지는 **차량 WiFi가 닿는 곳에서 한 번 켜서 시각을 맞춘 뒤 이동**하는 것이
> 오차를 줄이는 최선이다(§3의 마지막 시각 이어가기). 운영 문서에 이 절차를 적는다.
> 안 지켜도 데이터는 온전하고, 파일명의 `.unsynced`로 구분된다.
